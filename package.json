{
  "name": "tongkan",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "test:self": "pnpm --filter @tongkan/web test:self",
    "test:bridge": "pnpm --filter @tongkan/extension test:bridge",
    "test:integration": "node scripts/run-integration-tests.mjs",
    "deps:report": "node scripts/generate-dependency-inventory.mjs --write",
    "deps:check": "node scripts/generate-dependency-inventory.mjs --check",
    "release:prepare": "node scripts/prepare-production.mjs",
    "deploy:cloudflare": "node scripts/deploy-cloudflare.mjs",
    "android:test": "node scripts/run-android-gradle.mjs testDebugUnitTest",
    "android:lint": "node scripts/run-android-gradle.mjs lintDebug",
    "android:build": "node scripts/run-android-gradle.mjs assembleDebug",
    "android:check": "node scripts/run-android-gradle.mjs testDebugUnitTest lintDebug assembleDebug",
    "dev:web": "pnpm --filter @tongkan/web dev",
    "dev:signaling": "pnpm --filter @tongkan/signaling dev"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
