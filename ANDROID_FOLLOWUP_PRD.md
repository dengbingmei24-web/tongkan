# 同看 Android 后续开发 PRD

> 文档状态：讨论稿，不代表所有功能已确定
> 基线版本：`1.0.0-alpha.7`（versionCode 7）
> 创建日期：2026-08-07
> 核心原则：先听真实使用体验，再决定界面与功能；每次只解决最影响使用的问题。

---

## 1. 文档目的

这份 PRD 用于规划同看 Android 客户端在 Alpha 7 之后的开发工作，并作为产品讨论和使用反馈的共同记录。

它重点回答五个问题：

1. Alpha 7 之后最先解决什么问题？
2. 哪些功能是真正需要的，哪些暂时不要做？
3. Android App 的使用流程和界面应该怎样简化？
4. 如何把真实使用体验转化为明确的开发任务？
5. 什么条件满足后，才能认为 Android 版本可以长期自用？

本 PRD 不是一次性定稿。后续每次真实使用、问题复现和界面讨论，都可以继续补充并调整优先级。

## 2. 当前基线：Alpha 7

### 2.1 已具备能力

- 无注册、无登录创建双人临时房间。
- 通过邀请链接加入房间。
- Android 客户端使用 OkHttp 连接 Cloudflare 房间信令。
- 支持创建房间、加入房间、断线重连和房间状态恢复。
- 支持 BV、AV、分 P 和 `b23.tv` 短链接解析。
- App 内包含 B站 WebView 和播放器桥接脚本。
- 支持播放、暂停、拖动等同步协议。
- 已有中文错误提示和分阶段加载状态。
- 已有 Debug APK，可直接安装到 Android 8.0 及以上设备。

### 2.2 Alpha 7 的定位

Alpha 7 作为后续开发的稳定回退基线，重点保留：

- OkHttp WebSocket 连接方案。
- 当前房间协议和服务端兼容性。
- 已验证的创建、加入和重连流程。
- 当前 Android 原生 Java 工程结构。

后续播放器实验、界面重做和交互调整不能直接破坏 Alpha 7。高风险方案应先做独立验证，通过真机测试后再进入下一版本。

### 2.3 当前主要问题

| 问题 | 当前判断 | 优先级 |
|---|---|---|
| B站播放器桥接不稳定 | embed 页面中的视频可能位于跨域 iframe，父页面脚本无法访问 `<video>` | P0 |
| App 启动后直接出现视频界面 | 用户尚未创建或加入房间时就看到视频区域，流程顺序不自然，第一屏也不够美观 | P0 |
| 房间入口与观看界面混在一起 | 创建房间、邀请链接、视频链接、播放器和控制按钮同时展示，界面像调试工具 | P0 |
| 加载过程不够明确 | 用户可能不知道正在解析链接、等待房间、加载网页还是寻找播放器 | P0 |
| 失败后的下一步不清楚 | 有错误提示，但缺少“重试、换链接、重新连接”等直接动作 | P0 |
| 视频只能固定小屏观看 | 当前 WebView 高度固定约 220dp，没有全屏容器、横屏沉浸模式和全屏退出处理 | P0 |
| 真机体验数据不足 | 尚未形成固定的体验反馈和问题记录方式 | P0 |
| 正式签名缺失 | 当前 APK 使用 Debug 签名，只适合个人测试 | P2 |

### 2.4 已确认使用反馈（2026-08-07）

用户在 Alpha 7 上确认了两项直接影响日常使用的问题：

1. **打开 App 就直接进入视频界面，不够自然也不好看。** 用户期望先看到独立的房间入口页面，保留昵称输入，并提供“创建房间”和“粘贴邀请链接加入”两条路径。
2. **视频加载后只能在固定小区域观看，无法全屏。** 共同观看的核心场景需要沉浸式全屏，并由用户选择是否切换横屏。

由此确认下一版的界面顺序：

```text
启动 App
  → 房间入口页（昵称 / 创建房间 / 粘贴邀请链接）
  → 已进入房间
  → 视频链接与加载状态页
  → 视频观看页（支持全屏，并可手动选择横屏）
```

已确认的产品决策：

- 未创建或加入房间前，不显示 WebView、视频输入框、播放按钮和进度条。
- 创建或加入成功后，才切换到视频加载界面。
- 视频加载完成后，以播放器为视觉主体，房间和调试信息收起。
- 全屏观看从 P1 提升为 P0，是下一版核心需求，不再作为后期美化功能。
- 进入全屏时保持当前方向，不自动横屏；观看者可以通过明确按钮手动切换横屏。
- 返回键在全屏状态下优先退出全屏，不直接离开房间。

房间入口页排列已确认：

```text
昵称输入
[ 创建房间 ]
────── 或者 ──────
邀请链接输入
[ 加入房间 ]
```

“创建房间”是页面主操作，使用更突出的按钮；“加入房间”作为另一条完整但视觉级别稍低的路径。

推荐的工程实现是在同一个 `MainActivity` 中维护“房间入口、视频加载、共同观看”三个界面状态，避免为了页面分层引入新的复杂导航框架。

## 3. 产品目标

### 3.1 核心目标

让两个人可以用尽可能少的步骤进入房间、载入同一个 B站视频，并清楚知道当前是否连接、是否加载完成、是否已经同步。

### 3.2 体验目标

- 打开 App 后，用户能立即理解第一步该做什么。
- 创建者能够一键创建并分享邀请。
- 加入者点击邀请后，尽量自动进入房间。
- 粘贴 B站链接后，系统自动识别并显示明确加载进度。
- 视频应成为观看阶段的视觉主体。
- 普通状态使用自然语言，不显示不必要的技术细节。
- 出错时直接给出可执行的恢复按钮，而不只是报错文字。
- 任何“已连接”“已同步”“播放器就绪”状态都必须真实可信。

### 3.3 工程目标

- 保持原生 Java，不迁移 Kotlin、Compose 或复杂架构。
- 不引入非必要依赖。
- 播放器方案必须能够真机验证，不能只靠模拟和单元测试判断。
- 每个测试 APK 对应明确版本、改动说明和回退版本。
- 优先修复根因，不在界面上掩盖连接或播放器问题。

## 4. 非目标

下列事项暂不进入近期 Android 开发范围：

- 多人房间。
- 好友列表、多人社交和公开内容发现；邮箱验证码账号与唯一好友双人空间已转入 Alpha 10 主线。
- 公共房间或房间列表。
- Android 屏幕共享。
- 语音、视频通话和聊天。
- iOS 客户端。
- 应用商店发布。
- 复杂动画和大型设计系统。
- 支持所有视频网站。
- 服务器转发视频内容。

只有当“双人 B站同步观看”核心链路稳定、易用后，才重新讨论这些功能。

## 5. 后续开发阶段

### 阶段 A：真实使用反馈与问题排序

目标：先确定实际最难用、最影响观看的环节，不立即大改界面。

工作内容：

- 使用 Alpha 7 完成创建房间、加入房间、粘贴链接和加载视频。
- 记录每一步是否理解、是否等待过久、是否出现错误。
- 收集截图、错误文字、设备型号、Android 版本和网络环境。
- 区分“完全无法使用”“可以用但麻烦”“只是不好看”。
- 根据真实影响重新排列 P0、P1、P2。

阶段产物：

- 一份真实使用体验记录。
- 一份按严重程度排序的问题清单。
- 一份确认后的下一版本范围。

### 阶段 B：播放器技术方案验证

目标：找到能够稳定访问和控制真实视频元素的方案。

候选方案：

| 方案 | 优点 | 风险 | 当前建议 |
|---|---|---|---|
| A. 保留 `player.bilibili.com` embed 页面 | 页面相对专注，播放器入口明确 | 视频可能在跨域 iframe，桥接脚本无法控制 | 仅作为 Alpha 7 回退方案 |
| B. 加载 `www.bilibili.com/video/...` 移动页 | `<video>` 可能位于可访问 DOM，可继续使用现有桥 | 页面结构复杂，可能有跳转、登录提示、广告和 DOM 变化 | 建议做隔离真机实验 |
| C. 调起系统 B站 App | 用户观看体验成熟 | 无法直接访问播放器状态，难以实现同步控制 | 不作为核心方案 |
| D. 原生解析并播放视频流 | 播放控制最完整 | 接口变化、鉴权、版权、维护和兼容成本高 | 暂不采用 |

验证要求：

- 使用至少两个公开视频测试。
- 测试 BV、AV、分 P 和短链接。
- 能读取 `<video>`、时长、当前位置和暂停状态。
- 能执行播放、暂停和拖动。
- 页面跳转后桥接能够重新注入。
- 测试登录与未登录状态。
- 不通过验证的方案不得进入正式版本。

### 阶段 C：核心使用流程优化

目标：让 App 从“调试工具”变成“打开就知道怎么用”的个人产品。

建议流程：

1. 打开 App。
2. 选择“创建房间”，或手动粘贴邀请链接后点击“加入房间”。
3. 创建房间成功后自动弹出 Android 系统分享面板；视频准备页仍保留再次分享入口。
4. 任意一方粘贴 B站链接，不要求另一方已经加入。
5. App 自动识别、加载并把当前视频同步到房间。
6. 发起方本机准备完成后直接进入观看界面，不等待另一方播放器。
7. 已在线的另一方自动加载同一视频；稍后加入的人根据房间当前媒体自动同步。
8. 新视频停在 0 秒并保持暂停，等待用户主动播放。
9. 网络或播放器失败时，提供直接恢复操作。

需要讨论的流程问题：

- 昵称保留在房间入口页，不隐藏到设置中。
- 首页是否只保留“创建房间”和“加入邀请”两个主操作？
- **已确认**：App 启动时不自动读取剪贴板；用户点击邀请输入框内的“粘贴”后填入链接，再点击“加入房间”。
- **已确认**：粘贴 B站链接后只做格式识别和按钮启用，不自动加载；用户点击“准备视频”后才切换房间当前媒体。
- 房间连接成功后是否隐藏创建/加入区域？
- **已确认**：“离开房间”放在页面右上角更多菜单；视频准备页返回键和菜单离开操作都弹出确认。“重新连接”仅在连接失败或离线状态下显示，不作为常驻主按钮。

### 阶段 D：观看界面设计优化

目标：视频优先，状态清楚，控制简单。

建议界面分为四种状态，而不是把所有控件同时放在一个页面：

#### 状态 1：未进入房间

- 产品名称和一句简短说明。
- 主按钮：“创建房间”。
- 次按钮：“加入房间”。
- 邀请输入框提供用户主动触发的“粘贴”操作，不在页面进入时检测剪贴板。
- 昵称输入保留在入口页，作为创建或加入房间前的必要信息。
- 使用 `SharedPreferences` 保存最近一次非空昵称；下次打开自动填入，但始终可见且可修改。
- 昵称为空或只有空格时禁用“创建房间”和“加入房间”。

#### 状态 2：房间等待中

- 显示“房间已创建”或“已加入房间”。
- 清楚显示对方是否在线。
- 创建成功后立即弹出 Android 系统分享面板；取消分享不会离开房间。
- 创建者在视频准备页仍能看到明确的“分享邀请”入口，用于再次分享。
- 显示 B站链接输入区域。
- 对方未加入时不显示无效的播放控制。

#### 状态 3：视频加载中

- 使用一个统一的加载区域显示当前阶段：
  - 正在解析链接；
  - 已同步视频信息；
  - 正在打开 B站页面；
  - 正在寻找播放器；
  - 等待另一方准备完成。
- 提供“取消”和“重新加载”。
- 技术错误细节放入“查看详情”，默认只显示用户能理解的说明。

#### 状态 4：共同观看中

- 视频区域占据主要空间。
- 房间连接状态压缩为顶部小状态条。
- 常用控制只保留播放、暂停、进度和全屏。
- B站链接、邀请链接和调试信息默认收起。
- 支持全屏观看，并提供手动横屏按钮；全屏时保持屏幕常亮。
- 对方操作时给出轻量提示，不遮挡视频。

### 阶段 E：稳定性与长期自用

目标：核心流程可以反复使用，不需要频繁清数据或重新安装。

工作内容：

- 两部 Android 手机持续观看 30 分钟和 2 小时。
- Wi-Fi、移动网络以及网络切换测试。
- App 切后台、锁屏、恢复前台测试。
- WebView 页面重载和播放器重新注入测试。
- 房间过期、邀请错误、另一方离线测试。
- 使用 `SharedPreferences` 保存最近一次非空昵称，并验证下次启动自动填入、仍可编辑。
- 只保存当前会话短暂后台、锁屏和配置变化所需的恢复状态；冷启动不自动恢复旧房间。
- 为可长期安装的版本配置 Release 签名。

## 6. 功能优先级建议

### P0：下一阶段必须解决

- 确定可靠的 B站播放器控制方案。
- 将启动界面改为独立房间入口页，保留昵称输入、创建房间和邀请链接加入。
- 创建房间成功后自动弹出系统分享面板；取消分享不取消房间，准备页保留再次分享入口。
- 邀请链接由用户主动点击“粘贴”读取，不在 App 启动时自动访问剪贴板；填入后仍需点击“加入房间”。
- 进入房间后再显示视频链接和加载区域，未入房间时隐藏 WebView 与播放控制。
- 支持沉浸式全屏观看；进入全屏不自动横屏，用户可手动选择横屏，返回键安全退出全屏。
- 创建、加入、连接和加载状态真实准确。
- 用户可以明确知道卡在哪一步。
- 错误后有重试、重新连接或更换链接操作。
- 完成至少一次 Android ↔ Android 真机完整观看流程。
- 建立固定的使用反馈记录方式。

### P1：核心链路稳定后优化

- 加载成功后自动收起非观看控件。
- 观看时保持屏幕常亮。
- 更清楚的另一方在线、缓冲和操作提示。
- 最近使用的视频链接。

### P2：长期使用完善

- Release 正式签名和升级安装策略。
- App 内诊断信息导出。
- 更完整的设备兼容性清单。
- 图标、启动页和视觉细节统一。
- 可选的房间偏好和默认昵称设置。

### 暂不开发

- 多人、好友列表、聊天、语音、屏幕共享和多平台扩张；账号与唯一好友双人空间已单独进入 `ACCOUNT_PAIR_SPACE_PRD.md`。

## 7. 用户体验反馈机制

用户不需要按照技术格式反馈，可以直接描述感受。为了更容易转化为开发任务，建议每次体验包含以下信息。

### 7.1 最简反馈模板

```text
测试版本：Alpha 7
手机型号 / Android 版本：
使用网络：Wi-Fi / 移动网络
我想完成的事情：
我实际做了什么：
发生了什么：
我原本以为会发生什么：
最难受或最困惑的地方：
有没有截图或错误文字：
这个问题是否阻止继续使用：是 / 否
```

### 7.2 界面体验问题

可以直接回答下面这些问题，不需要一次全部回答：

1. 第一次打开 App 时，你是否知道先点哪里？
2. “创建房间”和“加入房间”是否容易理解？
3. 哪些文字像调试信息，对使用没有帮助？
4. 哪些按钮你从来不会点？
5. 哪些操作需要点太多次？
6. 创建房间后，你最想立刻看到什么？
7. 对方加入时，你希望 App 怎样提示？
8. 粘贴视频链接后，你希望自动加载还是手动确认？
9. 视频加载时，你最想知道什么信息？
10. 观看时，哪些控件应该一直显示，哪些应该隐藏？
11. 你是否需要横屏全屏？
12. 出错时，你更希望看到原因、解决按钮，还是两者都要？
13. 当前颜色、字号、间距和按钮大小哪里不舒服？
14. 哪个页面或状态最丑、最乱、最不自然？
15. 如果只能先改三个地方，你会选哪三个？

### 7.3 问题严重程度

| 等级 | 定义 | 示例 |
|---|---|---|
| S0 阻断 | 完全无法继续使用 | 无法启动、无法创建房间、无法播放 |
| S1 严重 | 可以绕过，但经常失败或非常麻烦 | 必须反复重试、经常丢失连接 |
| S2 体验 | 功能可用，但难理解或操作不顺 | 状态不明确、按钮层级混乱 |
| S3 视觉 | 不影响功能，但不美观或不统一 | 字号、颜色、间距、图标问题 |

开发优先处理 S0 和 S1，再根据使用频率处理 S2，最后统一 S3。

## 8. 用户故事

### 创建者

- 作为创建者，我希望一键创建房间并分享邀请，不需要理解房间密钥。
- 作为创建者，我希望清楚知道对方是否已经加入。
- 作为创建者，我希望粘贴 B站链接后双方自动进入同一视频。
- 作为创建者，我希望播放器失败时可以直接重新加载，而不是重新创建房间。

### 加入者

- 作为加入者，我希望手动粘贴邀请链接并点击加入，避免旧链接或误操作让我进入错误房间。
- 作为加入者，我希望知道自己正在连接、已经连接还是连接失败。
- 作为加入者，我希望对方载入视频后自动收到并加载。

### 共同观看

- 作为任意一方，我希望播放、暂停和拖动能同步到另一方。
- 作为任意一方，我希望知道对方正在缓冲还是已经离线。
- 作为任意一方，我希望观看时界面尽量少遮挡视频。
- 作为任意一方，我希望断网恢复后回到正确视频和进度。

## 9. 验收标准

### 9.1 房间流程

- 从打开 App 到创建房间不超过两个主要操作。
- 点击有效邀请后能够自动填充并加入。
- 房间状态只显示真实连接结果。
- 对方上线和离线在 5 秒内反映到界面；对方离线不自动暂停本机。
- 本机连接断开时立即暂停当前视频并进入自动重连状态。
- 自动重连成功后恢复房间状态；失败后显示可点击的“重新连接”。
- 失败时至少提供一个明确的恢复动作。

### 9.2 视频流程

- 支持至少两个不同的公开视频完成加载测试。
- 支持 BV 链接、AV 链接、短链接和分 P。
- 能读取真实时长、当前进度和播放状态。
- 播放、暂停和拖动可由任意一方发起。
- 稳定播放时双方偏差目标不超过 500ms。
- 页面重载后桥接能够恢复，不显示虚假的播放器就绪。

### 9.3 界面体验

- 每个页面只有一个明确的主要操作。
- 普通用户无需理解 WebSocket、锚点、DOM 或 iframe 等术语。
- 加载状态使用连续且可理解的阶段说明。
- 观看状态下视频是视觉主体。
- 未进入房间时不显示视频区域和播放控制。
- 点击全屏后以当前方向进入沉浸模式；用户可再点击横屏按钮切换方向，返回键退出全屏并保留房间连接。
- 常用操作适合单手点击，按钮触控区域不小于 48dp。
- 错误信息包含“发生了什么”和“接下来可以做什么”。

### 9.4 稳定性

- 连续执行 20 次播放、暂停和拖动，不出现控制循环。
- 切换 Wi-Fi 和移动网络后能够重连并恢复。
- App 切后台再恢复时不崩溃。
- 连续观看 30 分钟无阻断问题。
- 正式候选版本连续观看 2 小时无阻断问题。

## 10. 版本规划建议

| 版本 | 建议目标 | 是否确定 |
|---|---|---|
| Alpha 7 | OkHttp 网络基线和安全回退版本 | 已确定 |
| Alpha 8 | 已归档的 B站移动页实验，不再作为当前开发版本 | 已归档 |
| Alpha 9 | 核心：分阶段房间/观看流程、主题、B站弹幕、同步倍速、全屏和播放器稳定性；P1：房间互动表情弹幕 | 核心范围已确认，互动弹幕不阻塞核心构建 |
| Alpha 10 | 邮箱验证码账号、唯一好友、双人共享片库、日历、历史和统计 | 路线已由 D-059、D-063 更新，详见 `ACCOUNT_PAIR_SPACE_PRD.md` |
| Beta 1 | 双机核心流程稳定，开始使用 Release 签名 | 待讨论 |
| 1.0 | 满足双人长期自用验收标准 | 待讨论 |

版本号只代表测试阶段，不为了追求版本数量而增加功能。一个版本只解决一组可验证的问题。

## 11. 当前待讨论决策

### 已确认决策：入口流程与全屏

- App 第一屏是房间入口，不直接展示播放器。
- 房间入口保留昵称输入，不隐藏到设置。
- 最近一次非空昵称保存在本机并在下次启动自动填入，但始终可见、可编辑；空昵称时创建和加入按钮不可用。
- 入口页顺序已确认：昵称输入 → 创建房间 →“或者”分隔 → 邀请链接输入 → 加入房间。
- 创建房间成功后自动弹出 Android 系统分享面板；用户取消后仍停留在已创建房间，准备页保留再次分享入口。
- App 启动时不自动读取剪贴板；邀请输入框提供“粘贴”操作，填入后仍由用户点击“加入房间”。
- 创建或加入房间成功后，进入视频准备界面。
- 不要求双方同时在线或都准备完成；任一方都可以选择视频。
- 选择方本机准备完成后自动进入观看状态，另一方在线时自动加载，稍后加入时根据房间当前媒体同步。
- “准备视频”采用本机先验证、成功后广播的安全切换：选择方播放器桥接确认新视频可用后，才更新房间当前媒体。
- 准备期间对方继续观看原视频；准备失败不改变房间媒体，保留输入链接并提供重新加载或更换链接。
- 准备成功后双方显示“正在更换视频”，新视频从 0 秒暂停开始。
- 接收方收到房间新媒体后不弹确认，立即暂停旧视频、显示切换遮罩并自动加载；选择方不等待接收方。
- 接收方加载失败只影响本机，不自动恢复房间旧媒体；成功后应用房间当前进度、暂停状态和倍速，必要时追上已开始播放的另一方。
- 视频准备 0～8 秒显示正常加载；8 秒后显示慢速提示和“取消准备”；20 秒仍未确认播放器桥接可用时进入失败状态。
- 不显示虚假百分比；成功时短暂显示约 0.4 秒“准备完成 ✓”，然后广播房间切换。
- 粘贴 B站链接后不自动加载；识别为有效链接后启用“准备视频”按钮，由用户明确点击。
- 新视频从 0 秒暂停开始，等待用户主动播放。
- 下一版必须支持沉浸式全屏；不自动横屏，用户可手动选择横屏，返回键退出全屏。
- Alpha 9 已确认承载这组核心界面和观看流程改动；完整本地片单不进入 Alpha 9。
- 正常冷启动或彻底关闭后重新打开时进入房间入口页，不自动进入旧房间。
- 当前会话切后台、锁屏、短暂系统恢复或横竖屏切换时继续当前房间和页面。
- 全屏状态按返回键先退出全屏；普通观看页按返回键回到视频准备页，保持房间连接且不重新加载视频。
- 视频准备页按返回键时弹出“确定离开房间吗？”；“离开房间”同时位于右上角更多菜单并使用相同确认。
- 用户确认离开后断开连接、清除当前会话状态并返回入口页。
- “重新连接”只在连接失败或离线状态出现，正常连接时不常驻。
- 只有对方离线、本机仍连接房间时继续本机播放，并显示“对方已离线，等待重新加入”；对方回来后自动同步当前视频和进度。
- 本机与房间连接断开时自动暂停视频，显示“连接已断开，正在重新连接”；重连成功后同步房间状态。
- 自动重连失败后显示明确的“重新连接”按钮，不要求用户离开或重新创建房间。

### 决策 1：Alpha 9 核心优先级（已确认）

Alpha 9 同时解决分阶段入口、全屏观看、播放器稳定、连接恢复和自然语言错误；旧“Alpha 10 纯本地片单”顺序已被 D-059 取代，互动表情仍作为不阻塞核心的 P1。

### 决策 2：B站最终播放器方案（Alpha 9 核心唯一待定项）

需要确认：

- Alpha 8 的实际安装和使用结果。
- 移动页面是否出现登录提示、跳转或无法播放。
- 桥接脚本是否真正找到 `<video>`。
- 与 Alpha 7 相比，是更接近可用还是更不稳定。

在没有真机证据前，不把移动页面方案视为已完成方案。

### 决策 3：界面产品化方向（已确认）

采用 C · 简洁工具感：普通界面产品化，技术信息放入折叠的“查看详情”，默认浅色并支持黑灰深色。

### 决策 4：链接载入方式（已确认）

粘贴只识别链接并启用“准备视频”，必须由用户明确点击；选择方本机验证成功后才广播房间媒体切换。

### 决策 5：全屏方向如何控制？（已确认）

- 全屏是下一版 P0，不再延后。
- 点击全屏时保持当前屏幕方向，不强制自动横屏。
- 全屏界面提供明确的“横屏”切换操作，由用户自行选择。
- 退出全屏后恢复进入全屏前的方向和界面状态。
- 全屏使用纯黑背景，点击视频区域切换控制栏显示；播放且无操作 3 秒后自动隐藏。
- 暂停、加载、拖动进度或发生错误时控制栏保持显示；交互结束且继续播放后重新计时。
- 全屏控制栏保留退出全屏、播放/暂停、进度和横屏/竖屏切换。
- 正常同步只显示小型状态图标；对方离线或本机断线时才显示明显提示。

## 12. 讨论与变更流程

1. 你描述一次真实使用体验，可以是口语化感受。
2. 将反馈拆成阻断问题、操作问题、界面问题和视觉问题。
3. 确认问题是否可稳定复现。
4. 把问题写入本 PRD，并调整优先级。
5. 双方确认下一测试版只解决哪些问题。
6. 修改代码并生成新 APK。
7. 使用同一组步骤复测，对比修改前后的体验。
8. 将结果更新到 `CONTEXT.md` 和本 PRD。

## 13. 下一次讨论建议

下一次可以先不谈技术实现，直接从你的实际体验开始。建议你按使用顺序描述：

1. 打开 App 第一眼看到了什么、觉得应该做什么。
2. 创建或加入房间时哪里顺利、哪里困惑。
3. 粘贴 B站链接后发生了什么。
4. 哪一步等待最久或最没有安全感。
5. 观看区域和控制按钮是否符合习惯。
6. 发生错误时，你希望 App 怎样帮助你恢复。
7. 如果今天只能修改三个地方，你最希望改哪三个。

这些回答将用于确定下一版本，而不是预先假设用户需要哪些功能。
## 14. UI/UX 设计介入时机

### 14.1 结论

界面设计不应该全部留到开发最后，也不应该在核心功能尚未确定时先做大量视觉细节。

同看 Android 采用“两阶段设计”方式：

1. **现在完成 UX 和视觉方向设计。** 确定页面顺序、信息层级、按钮主次、颜色方向、字体层级、间距、圆角和状态反馈，避免继续在不喜欢的旧界面上叠加功能。
2. **核心功能稳定后完成视觉精修。** 调整动画、过渡、阴影、图标细节、不同设备适配和最终一致性，不让视觉微调阻塞播放器与同步功能验证。

### 14.2 现在需要设计的内容

- 房间入口页的整体视觉风格。
- 昵称、创建房间、分隔线、邀请链接和加入房间的层级与间距。
- 创建或加入成功后的页面切换方式。
- 视频加载页的状态展示和错误恢复操作。
- 共同观看页的视频占比、顶部状态和底部控制。
- 全屏、手动横屏、退出全屏等操作的位置与图标。
- 全局颜色、字体、按钮、输入框、卡片、背景和状态色规则。
- 触控区域、对比度、安全区和 Android 返回键行为。

### 14.3 后期再精修的内容

- 页面切换动画和微交互。
- 按钮按压、加载骨架和成功提示的细节。
- 图标风格的最终统一。
- 不同屏幕尺寸和厂商设备上的像素级调整。
- 启动页、App 图标和品牌装饰元素。
- Beta 或 1.0 发布前的最终视觉质量检查。

### 14.4 `ui-ux-pro-max` 技能使用方式

已安装 `ui-ux-pro-max` 技能，用于下一轮界面讨论和设计系统生成。该技能主要作为设计决策与质量检查工具，不改变项目的原生 Java 技术路线。

计划用法：

1. 输入产品类型、使用场景和期望气质，生成 2～3 套候选视觉方向。
2. 分别查询移动端娱乐产品、双人私密场景、暗色观看界面、表单和触控交互建议。
3. 对比候选方案的颜色、字体、层级、密度和可访问性。
4. 与用户共同选择一套方向，不由技能自动决定最终风格。
5. 为房间入口、视频加载、共同观看和全屏四种状态生成统一界面规范。
6. 实现后再次用技能检查触控尺寸、对比度、状态反馈和一致性。

由于当前 Android 客户端使用原生 Java 动态构建界面，而技能没有专门的 Android Java 实现模板，因此采用其通用移动端 UX、色彩、排版和交互建议，再按照现有 Java 工程手动实现，不引入 Compose。

### 14.5 下一轮设计工作坊

下一轮不直接修改代码，先使用技能输出三种明显不同的方向供讨论：

- **方向 A：沉浸式影院感**——深色、视频优先、低干扰、强调共同观看状态。
- **方向 B：温暖双人感**——更柔和、更亲密，突出“我和对方正在一起看”。
- **方向 C：简洁工具感**——信息清晰、操作直接、装饰少但不再像调试页面。

讨论时选择一套主方向，也可以从不同方向中组合少量元素。方向确认后，先形成文字版设计系统和页面线框，再进入 Alpha 9 实现。
### 14.6 第一版可交互预览

已生成网页端可交互设计原型，用于快速比较视觉方向和页面流程：

- 原型：`design/alpha9-ui/preview.html`
- 设计方向说明：`design/alpha9-ui/DESIGN_DIRECTIONS.md`
- 方向 A 截图：`design/alpha9-ui/preview-cinema.png`
- 方向 B 截图：`design/alpha9-ui/preview-warm.png`
- 方向 C 截图：`design/alpha9-ui/preview-minimal.png`
- 手动横屏截图：`design/alpha9-ui/preview-landscape.png`

原型支持：

- 三套视觉方向切换；
- 房间入口、视频加载和共同观看三个界面状态切换；
- 从入口按钮进入后续页面；
- 全屏保持当前方向；
- 单独点击“横屏”后切换横屏布局；
- 返回按钮退出全屏并清除横屏状态。

自动验证结果：页面状态切换正常，全屏不会自动横屏，手动横屏正常，退出全屏正常，390px 手机视口没有水平溢出。

该 HTML 只用于设计讨论和交互验证。最终 Alpha 9 UI 仍然在原生 Android Java 中实现，不会把此网页直接作为 App 界面。
### 14.7 视觉方向选择结果

用户已选择 **C · 简洁工具感** 作为 Alpha 9 主设计方向。

确认内容：

- 米白页面背景、白色表面、石墨色主要按钮和灰绿色状态强调。
- 信息层级清楚，避免霓虹、复杂渐变、粉色恋爱软件感和调试工具感。
- 输入框保留可见标签，按钮主次明确，所有触控区域满足 Android 48dp 最小尺寸。
- 房间入口和视频加载页使用浅色简洁界面。
- 原型默认打开 C 方向，不再并行推进 A、B 的实现。

已生成详细规范：`design/alpha9-ui/SELECTED_DESIGN.md`。

浅色和深色模式均进入 Alpha 9 范围：

- 默认浅色；
- 用户可以主动切换深色；
- 进入观看页时不自动切换主题；
- 选择结果保存，下次启动继续使用；
- 主题切换不影响房间连接和视频播放。

圆角规则同步优化：主要按钮、次要按钮和输入框使用 12dp，普通卡片使用 16dp，小图标按钮使用 12dp；胶囊圆角只用于在线状态等短标签。

已更新预览：`preview-selected-light.png`、`preview-selected-dark.png` 和 `preview-watch-dark.png`。主题切换入口已确定：房间入口、视频加载和共同观看三个页面均放在右上角，使用太阳/月亮线性图标直接切换；图标触控区为 48×48dp，切换时不重建页面、不影响房间连接和视频播放。

### 14.7.1 房间创建、加入与迟到加入

- 创建或加入按钮点击后立即显示加载状态、锁定相关输入并禁止重复点击。
- 创建成功后底层先进入视频准备页，再自动弹一次 Android 系统分享面板；取消分享不退出房间，页面保留再次分享入口。
- 加入失败区分邀请无法识别、邀请已失效、房间已满和网络暂时失败，并分别提供直接恢复操作。
- 创建或加入超过 8 秒显示慢速提示，15 秒后进入可恢复失败，可取消返回入口页。
- 加入成功且房间没有当前媒体时进入视频准备页；如果房间已有媒体，迟到加入者自动加载并应用当前进度、暂停状态和倍速。
- 迟到加入者加载失败只在本机重试，不修改房间媒体。

### 14.7.2 房间成员状态与邀请复用

- 房间只有自己时显示“等待对方加入”，但允许选择视频、进入观看和独自播放。
- 对方加入、离开和重新加入使用约 2 秒轻量提示；双方在线时使用绿色小状态，等待和离线使用中性灰。
- 对方离线或主动离开不暂停本机；对方重新加入后自动同步当前媒体和播放锚点。
- 同一邀请链接在房间仍有效期间允许重新加入，不要求房主重新创建和分享。
- 只有本机连接断开才自动暂停并显示明显重连状态。

### 14.7.3 离开房间与空置过期

- 主动离开确认显示“离开后将停止同步。房间无人连接 10 分钟后会自动失效”，操作为“继续观看”和“离开房间”。
- 确认离开后仅断开本机、清除本机会话并返回入口，不主动关闭另一方的房间。
- 只要仍有一方连接，房间不进入过期；双方都断开后保留 10 分钟，期间原凭据可以重新连接。
- 超过 10 分钟房间自动删除，旧邀请显示“邀请链接已经失效”。Alpha 9 不增加“强制解散房间”。

### 14.8 视频准备完成后的进入方式

- 用户点击加载操作后，界面显示明确的识别、打开和播放器准备状态。
- 视频准备成功后自动进入共同观看页面，不再增加“进入观看”的二次确认按钮。
- 如果准备失败，则停留在视频加载页，保留原链接并提供重新加载或更换链接。
- 加载过程中不向用户显示 WebView、DOM、桥接脚本等实现术语。

### 14.9 按钮按下、加载和结果反馈

按钮必须通过三个阶段证明操作已经被接收：

1. **按下阶段**：50ms 内改变背景色约 10%–15%，显示受控波纹，可叠加不超过 0.98 的轻微缩放，但不能只依赖缩放。
2. **执行阶段**：立即显示旋转进度图标和操作文案，例如“正在创建…”“正在加入…”“正在准备视频…”，并禁用重复点击。
3. **结果阶段**：成功时短暂显示对勾和完成文案，约 300ms 后进入下一页面；失败时恢复按钮并在附近显示可操作的错误提示。

补充约束：

- 浅色主按钮按下色参考 `#383A36`，深色主按钮按下色参考 `#D9DED7`。
- 不使用夸张弹跳、明显位移或长时间动画。
- 不把短暂 Toast 作为唯一反馈。
- Android 原生实现优先使用 `RippleDrawable` 和状态颜色，不增加依赖。
- 视频准备成功后自动进入共同观看页面。

### 14.9.1 播放缓冲与双方等待

- 本机缓冲不超过 2 秒时不通知房间，不显示明显提示。
- 持续超过 2 秒后，本机只发送一次 playback.report buffering=true，服务端暂停房间；卡顿方显示“正在缓冲”，另一方显示昵称和“已一起暂停”。
- 缓冲恢复后发送 buffering=false，但不自动恢复播放；任意一方手动播放后双方继续。
- 持续超过 15 秒时，卡顿方提供“重新加载播放器”和“在 B站打开”，另一方保持暂停并显示对方仍在加载。
- Alpha 9.3.2 已在 Android 端加入 2 秒防抖、恢复取消和单次触发保护：短暂缓冲不通知房间，持续缓冲每个 episode 只上报一次，恢复也只上报一次；仍需双机真机验证。

### 14.9.2 进度偏差纠正

- 小于 0.3 秒的偏差忽略。
- 0.3～1.5 秒偏差且房间正在播放时，临时将本机倍速调整到房间倍速正负最多 0.05，并在约 2.5 秒后恢复。
- 超过 1.5 秒的偏差直接 seek 到房间当前位置，并显示约 1 秒“已重新同步”。
- 后台恢复、锁屏恢复、重连和新视频加载完成后直接应用房间当前锚点。
- 现有播放器桥接已经实现阈值、临时倍速和硬跳转，Alpha 9 需要补充提示、状态抑制和真机验证。

### 14.9.3 视频播放结束

- 视频自然结束后双方停在末尾，显示“视频播放结束”遮罩。
- “重新播放”同步双方回到 0 秒并开始播放；“选择其他视频”只让点击者进入准备页，另一方保留结束页并看到对方正在选择。
- 新视频准备失败不改变房间媒体，准备成功后才广播切换。
- Alpha 9 不自动重播、播放推荐内容、切下一集或退出房间；Alpha 10 片单版本再增加“播放下一个”和“返回片单”。

### 14.10 错误文案与恢复操作

- 默认错误卡片只显示用户能理解的原因、一个主要恢复操作和必要的次要操作。
- 链接无法识别：显示“这个链接不是可识别的 B站视频”，主要操作为“重新粘贴”，不进入加载流程。
- 网络或加载超时：显示“视频还没有加载出来”，说明网络可能不稳定，主要操作为“重新加载”，次要操作为“更换链接”。
- 网页已打开但播放器桥接失败：显示“播放器没有准备好”，主要操作为“重新加载播放器”，次要操作为“在 B站打开”。
- 视频失效、需要登录、地区受限或无法在 WebView 播放：显示“这个视频暂时无法在同看中播放”，主要操作为“更换链接”，次要操作为“在 B站打开”。
- 房间连接断开不属于视频错误：本机立即暂停并自动重连；自动重连失败后才显示“重新连接”。
- 任何准备失败都保留原始链接、房间连接和可用的原视频，不自动清空输入、不要求重新创建房间。
- WebView、DOM、JavaScript、WebSocket、异常堆栈和内部错误码默认收进“查看详情”，用于排查但不作为主文案。

### 14.11 弹幕与倍速（新增确认需求）

- Alpha 9 必须提供弹幕显示选项，观看时可以看到 B站弹幕并通过 App 控制开关。
- 播放器实现不能再固定使用 `danmaku=0` 关闭弹幕；应保留 B站播放器的视频与弹幕显示层，隐藏评论、推荐和页面级杂项。
- 弹幕开关由 Android 原生控制栏提供，底层通过桥接控制 B站弹幕层；Alpha 9 首个可用构建已加入桌面 Embed 弹幕桥接和本机持久化开关；仍需真机验证不同视频上的 DOM 稳定性。
- Alpha 9 必须提供倍速选择；现有房间协议、`PlaybackAnchor.playbackRate`、Android `rate` 命令和播放器桥接已经具备同步基础。
- 倍速入口放入原生观看控制栏，修改后通过现有 `playback.command kind=rate` 同步给房间。
- 弹幕默认开启，属于每台手机的独立偏好，不同步给对方；使用 `SharedPreferences` 保存，下次启动沿用。
- 弹幕不可用或桥接失败时将开关禁用并显示“弹幕不可用”，不影响视频播放。
- 倍速档位固定为 `0.5×`、`0.75×`、`1.0×`、`1.25×`、`1.5×`、`2.0×`。
- 每个新房间初始倍速为 `1.0×`，不沿用上一个房间的倍速；房间快照中的当前倍速用于重连和稍后加入。
- 任意一方改变倍速后通过现有协议同步双方；另一方显示轻量提示，例如“对方已切换为 1.5×”。
- 控制栏显示当前倍速按钮，例如 `1.0×`；点击后打开紧凑选择面板。

### 14.12 房间表情弹幕（新增讨论）

用户希望除了观看 B站弹幕外，还能在页面上发送一套预设表情，让房间里的两个人实时看到。该功能不发送文字，也不依赖 B站账号。

当前技术判断：

- 功能可行，建议命名为“房间表情弹幕”或“互动表情”，与 B站弹幕明确区分。
- 第一版可以复用现有 `chat.message` 广播、昵称和限流，不必新增 Durable Object 持久化字段或播放协议。
- Android 发送稳定 Emoji 标识，双方客户端把标识映射为内置表情动画；Web 端即使未适配也至少能显示普通 Emoji。
- 表情只实时广播，不保存、不进入房间快照、不补发给离线用户，也不绑定视频时间轴。
- 表情覆盖层位于 App 原生视频容器之上，与 B站弹幕层分离；应限制轨道、同时显示数量和动画时间，避免遮挡视频。
- 现有聊天限流允许短时 5 条、之后约每秒 1 条，适合第一版防止连续刷屏。
- Android 当前尚未实现 `chat.message` 发送和接收，需要增加 RoomClient 回调、原生表情面板和覆盖动画。

已确认第一版只提供 8～12 个内置表情，不支持文字、自定义图片、历史记录或离线补发。该功能进入 Alpha 9 P1：核心入口、播放器、全屏、B站弹幕和倍速稳定后再实现；如果影响核心稳定性，可以延期，但不得阻塞 Alpha 9 核心 APK 构建。

美术方向已确认采用黑白线稿“小团子”：白色圆润身体、粗黑轮廓和黑色五官，黄色/红色/蓝色仅作少量情绪点缀，不使用绿色主色、文字、复杂渐变或立体高光。首套固定 10 个：哈哈、震惊、好耶、点赞、鼓掌、爱心、抱抱、委屈、生气、困了。资产保留 `256×256px` 透明 PNG 母版，Android 打包使用无损 WebP。

互动显示规则：

- B站弹幕和互动弹幕使用两个独立的本机开关，互不影响。
- “弹幕”控制 B站原视频弹幕；“显示互动弹幕”控制双方表情动画，两个开关都默认开启并分别持久化。
- 控制栏的“互动”按钮只负责打开表情面板和发送，不承担显示开关。
- 表情面板锚定在“互动”按钮上方，使用小型两行网格（第一版 10 个表情，每行 5 个），不放在视频顶部、不展开为大面板，避免遮挡主要画面。
- 点击表情后立即关闭面板；点击外部同样关闭。面板打开期间保持控制栏可见并暂停自动隐藏倒计时。
- 自己发送后本机立即显示动画；服务器广播用于让对方显示，需要用 `messageId` 避免发送者收到回声后重复播放。
- 对方表情可带小型昵称标识；每条约显示 2.5 秒，同时最多显示 3 条，多余内容进入短队列。
- 动画采用右侧短距离滑入，不横穿全屏：竖屏约 `48dp`，全屏/横屏约 `56dp`，向左移动约 `120dp` 并轻微上浮后淡出。
- 三条显示轨道位于视频上半至中部，底部约 30% 作为字幕与控制安全区；自己发送的不显示昵称，对方表情显示小型昵称。
- 点击发送时“互动”按钮短暂缩放和变色，并调用轻触触觉反馈；不播放声音。
- 全屏控制栏隐藏时互动弹幕仍然显示；关闭互动弹幕只影响本机显示，不阻止发送，也不改变对方设置。

## 15. 播放列表与视频切换（讨论稿）

### 15.1 用户需求

- 一次添加多个 B站视频，减少观看过程中反复复制和粘贴链接。
- 以列表形式显示每个视频的封面、标题和基础信息，便于按需求选择。
- 支持调整顺序、删除和选择某个视频播放。
- 切换视频时，两端都显示明确的“正在更换视频”状态，避免误以为按钮无效或播放器卡住。

### 15.2 推荐界面结构

- 视频加载页保留单个链接输入，同时增加“批量添加”入口；批量模式支持一行一个链接。
- 页面下方显示“播放列表（N）”，当前视频使用描边和“正在播放”状态突出。
- 每项包含：16:9 封面、标题、BV/分P信息、明确的播放按钮和更多菜单；点击播放按钮才切换视频，避免误触整张卡片。
- 元数据加载期间显示骨架占位；封面或标题获取失败时仍保留 BV 号和可播放操作。
- 普通模式隐藏拖动控件；点击片单右上角“排序”后进入编辑模式，每项显示拖动把手，支持长按上下拖动并在松手后自动保存。更多菜单同时提供移到顶部、上移、下移和删除作为替代操作。

### 15.3 视频切换反馈

选择新视频后，当前播放器区域立即进入切换遮罩并禁用播放控制：

1. `正在更换视频…`
2. 显示目标视频封面和标题。
3. 状态依次为“正在通知对方”“正在加载本机播放器”“本机准备完成”。
4. 本机准备完成后隐藏遮罩并进入观看页，不等待对方；对方在线时异步加载，稍后加入时同步当前视频。新视频停在 0 秒且保持暂停，等待用户主动点击播放。
5. 失败时显示“更换失败”，保留目标视频并提供重新加载；是否自动恢复上一视频待技术验证。

### 15.4 实现范围选择

**方案 A：本机播放列表（已选择）**

- 列表保存在 Android 本地，重新打开 App、退出房间或创建新房间后仍可使用。
- 选择某一项时继续使用现有 `media-change` 通知房间，对方只接收当前视频变化。
- 不修改信令协议，风险和开发量较低，适合在播放器稳定后优先实现。
- 限制：对方看不到完整列表，也不能共同编辑排序。

**方案 B：房间共享播放列表**

- 列表、排序和当前项存入 Durable Object，双方看到同一份队列。
- 需要新增列表增加、删除、移动和选择协议，并处理双方同时编辑冲突。
- 可进一步支持双方共同添加视频，但开发和测试范围明显更大。

### 15.5 当前技术判断

- 现有 `playback.command` 的 `media-change` 已能承担选择列表项后的实际视频切换。
- 封面和标题需要增加 B站视频元数据解析与本地缓存，并提供获取失败时的降级界面。
- Android 当前未引入 RecyclerView、图片加载库或 Compose；第一版应优先使用现有 Java View、OkHttp 和轻量本地 JSON 存储，不为列表单独引入大型依赖。
- 播放列表不与 Alpha 9 核心界面、全屏和播放器稳定性混在同一版本实现；原“Alpha 10 纯本地片单”规划已被 D-059 的账号与双人空间路线取代。

### 15.6 待确认

1. **已确认**：第一版只保存在自己的手机，长期保留；对方不显示完整列表，只跟随当前视频切换。
2. **已确认**：新视频准备完成后自动进入观看页，但停在 0 秒并保持暂停，等待用户手动播放。
3. **已确认**：普通模式保持简洁；点击“排序”进入编辑模式后显示拖动把手，支持长按拖动，菜单保留移到顶部、上移、下移和删除。
4. **已确认**：第一版不自动播放下一个；播放结束后显示“播放下一个”和“返回片单”，自动连播可作为以后默认关闭的选项。
5. **已更新**：完整片单不进入 Alpha 9；Alpha 10 改为先建立账号与唯一好友，再交付双人共享片库，详见 D-059 和 `ACCOUNT_PAIR_SPACE_PRD.md`。

### 15.7 已确认范围

- 第一版实现手机本地长期片单，不实现双方共享编辑。
- 片单独立于房间存在，退出房间、创建新房间或重启 App 后继续保留。
- 本机负责添加、删除、排序和选择视频；选择后通过现有 `media-change` 同步当前视频给对方。
- 对方只接收正在播放的视频变化，不接收整份片单内容。
- 视频标题、封面和解析结果在本机缓存；缓存失败时保留链接和 BV 号作为降级信息。
- 第一版不修改 Cloudflare Durable Object 的播放列表协议。
- 视频切换准备完成后自动进入观看页，但新视频停在 0 秒并保持暂停，等待用户手动播放。
- 第一版不自动连播；播放结束后显示“播放下一个”和“返回片单”，由用户决定下一步。
- 片单普通模式不显示拖动控件；点击“排序”后进入编辑模式并显示拖动把手。
- 长按拖动时提供轻微触觉反馈和卡片浮起效果，松手后立即保存顺序。
- 每项更多菜单保留移到顶部、上移、下移和删除；删除正在播放的视频不会中断当前播放。
- 视频卡片使用独立播放按钮触发切换，点击卡片其他区域不会直接更换双方视频。

### 15.8 片单入口与批量添加（已确认）

- 房间入口页保持简洁，不显示片单入口。
- 创建或加入房间后，“准备视频”页面直接展示“我的片单”。
- 共同观看页面右上角提供片单图标；点击后从底部弹出片单，不退出观看页、不断开房间、不重新加载当前视频。
- 点击“添加视频”打开底部面板，支持一次粘贴多条链接，每行一个。
- 解析完成后先显示结果摘要，例如“识别到 6 个视频，1 个重复”，再由用户确认添加。
- 确认按钮使用实际数量，例如“添加 5 个视频”。
- 重复视频不再次添加；无法获取封面或标题的项目保留链接和 BV 号，并支持单独重试。
- 添加完成后统一在后台加载标题和封面，不阻塞返回片单或当前视频播放。

### 15.9 片单交互预览

已创建独立交互原型：`design/alpha9-ui/playlist-preview.html`。

原型可切换以下状态：

- 我的片单：封面、标题、UP主、时长、分P、正在播放、已看完和元数据失败降级。
- 排序模式：隐藏播放按钮并显示拖动把手。
- 批量添加：多行链接、重复识别和确认添加数量。
- 观看时片单：在深色观看页底部弹出，不退出当前视频。
- 正在更换：目标封面、标题和三阶段加载反馈。
- 播放完成：显示“播放下一个”和“返回片单”，不自动连播。

验证结果：

- 默认浅色和用户选择深色均正常。
- 五张视频卡片和五个排序把手均正确显示。
- 批量添加、观看片单、更换视频和播放完成状态均可打开。
- 390px 手机内容宽度无横向溢出。

截图：

- `design/alpha9-ui/preview-playlist-light.png`
- `design/alpha9-ui/preview-playlist-sort.png`
- `design/alpha9-ui/preview-playlist-batch.png`
- `design/alpha9-ui/preview-playlist-watch-dark.png`
- `design/alpha9-ui/preview-playlist-switching.png`

“已看完”标签目前作为预览候选展示，是否进入第一版仍待用户确认。

### 14.10 深色主题颜色修正

用户反馈原深色方案呈现明显墨绿色，不符合“深色应该是黑色”的预期。深色模式调整为：

- 页面背景使用接近纯黑的 `#0B0C0D`。
- 卡片和浮层使用中性炭灰 `#171819`，次级表面使用 `#242528`。
- 边框使用中性灰 `#303236`，选中和同步强调使用 `#94969B`。
- 视频占位环境改为黑色和石墨灰渐变，不再使用绿色渐变。
- 绿色只保留给在线、成功等语义状态的小面积提示，不参与页面主色。


## 16. Alpha 9 首个可用构建状态（2026-08-07）

### 16.1 已进入首个可用 APK

- 版本：`1.0.0-alpha.9`，`versionCode 9`。
- 播放器：桌面 UA + 顶层 `player.bilibili.com/player.html`，默认启用 B站原生弹幕。
- 页面顺序：正常启动先显示房间入口；创建或加入成功后进入视频准备与观看区域。
- 视频切换：选择方先本机加载并确认播放器可用，再发送 `media-change`；成功后停在 0 秒等待播放。
- 观看控制：本机弹幕开关、六档同步倍速、手动横屏、App 沉浸全屏和 B站 HTML 全屏回调。
- 主题：默认浅色，可无断线切换黑灰深色；按钮提供明确的按下变色反馈。
- 房间：创建成功进入房间后自动弹出系统分享面板；离开房间前显示确认。
- 加载：8 秒慢提示、20 秒超时和取消准备已进入首个构建。
- 验证：15 项单元测试、Android Lint 和 Debug APK 构建全部通过。

### 16.2 为赶在首个可用版本后置的优化

- 持续缓冲 2 秒防抖和一次性上报保护已在 Alpha 9.3.2 实现，等待双机真机验收。
- 四类错误的完整卡片、折叠技术详情和全部恢复按钮仍需继续完善。
- 播放结束遮罩、重新播放/选择其他视频的完整状态页仍需继续完善。
- 全屏控制栏 3 秒自动隐藏、硬同步提示和更细的成员状态动画仍属于稳定化优化。
- 10 个双人互动表情继续保持 Alpha 9 P1，不阻塞首个核心 APK。
- 旧长期本地片单方案已并入 Alpha 10 双人共享片库阶段，不再作为独立的纯本地里程碑。

### 16.3 首个 APK

- 路径：`release/tongkan-android-1.0-alpha9.apk`
- SHA-256：`B530C274A5B11F1C6D3CB9635957FE9B2BC35194907C5F3C9FDBED42926956D9`
- 当前没有连接的 ADB 设备，因此本轮完成构建验证，但尚未完成真机安装和双机联调。


### 16.4 首轮真机反馈与 Alpha 9.1 修复

首轮安装确认：房间入口顺序、深浅主题、创建后自动分享、B站视频加载播放和原生弹幕均已基本可用。发现以下阻断问题：

1. 页面没有处理刘海/灵动岛安全区，标题和观看页按钮被系统状态栏遮挡。
2. Android Button 默认 elevation 造成不自然阴影，视觉不符合简洁工具感。
3. WebView 使用剩余高度铺满，竖屏观看时播放器纵向占满页面，原生控制栏被挤出屏幕。
4. B站播放器内标题、作者和推广链接会把用户带离 App。
5. 弹幕、倍速和横屏按钮因为位于屏幕下方不可见，无法正常操作。

Alpha 9.1 已完成：

- 根据真实系统 Window Insets 增加顶部与底部安全区。
- 移除按钮系统 elevation 和默认状态动画，只保留明确的按下变色反馈。
- 普通观看页改为居中的 16:9 播放器；上方显示房间状态和在线人数，下方恢复原生控制栏。
- 屏蔽 B站播放器内部链接、`window.open`、标题作者覆盖层、推荐和“进入哔哩哔哩”覆盖层。
- 横屏按钮由用户主动触发，并进入沉浸观看；返回键仍优先退出全屏。
- 真实桌面 Embed 探针验证：页面 URL 不再变化、弹幕从开启切换为关闭、倍速成功切换到 1.5×。
- 版本：`1.0.0-alpha.9.1`，`versionCode 10`。
- APK：`release/tongkan-android-1.0-alpha9.1.apk`。
- SHA-256：`1AFC421B94F0CB970D23A4396CB274F07B025B3AA94AE3085021CB3A97960885`。

### 16.5 待讨论：观看页底部互动区

用户提出在播放器下方增加聊天框。下一轮需要在以下方向中确认：

- 仅保留之前确认的紧凑互动表情按钮，不加入键盘文字输入。
- 增加可收起的单行文字聊天，默认只显示“说句话”入口，点击后再弹出键盘，避免长期压缩播放器。
- 同时提供“互动表情 + 说句话”，但控制栏必须保持紧凑，文字消息是否在视频上短暂显示也需要单独决定。

在 Alpha 9.1 真机确认播放器、弹幕、倍速和横屏正常前，不实施聊天区，避免同时修改播放器和房间消息协议。

### 16.6 Alpha 9.2 视觉升级决策

用户接受 ui-ux-pro-max 审计后的推荐方案，并要求把入口页主标题替换为每日轮换的一句经典电影台词。

- 视觉方向为“安静的观影工具”：纯黑白灰为主，绿色只表达在线/成功，不使用装饰性阴影。
- 房间入口取消单一大卡片，改为开放式布局、创建与加入两个独立区块，以及轻量的上次房间入口。
- 入口页每日台词随 APK 本地内置，按设备本地日期确定；同一天固定不变，跨过本地午夜后更新，台词下方显示电影名。
- 观看页顶部只保留返回、房间状态、分享、更多；换视频和主题切换进入更多菜单。
- 播放器下方重排为状态、进度、主要播放控制和弹幕/倍速/横屏/全屏四个图标工具。
- 下方预留收起状态的“互动 / 说句话”入口，具体聊天协议和消息展示方式仍不在本轮视觉实现中冻结。
- V2 需分别验证浅色入口、浅色观看、深色观看和横屏观看，并确保 48dp 触控区、8dp 间距、安全区、浅深色对比度和 150～250ms 按压反馈。

### 16.7 Alpha 9.2 V3 安全区与横屏修订

用户对 V2 预览提出两项修订：竖屏整体仍需下移，横屏不应保留右侧房间控制栏。

- 竖屏页面使用 `systemInsetTop + 16dp` 作为顶部基础留白，底部继续叠加系统导航/手势安全区。
- 横屏进入真正的沉浸播放器，视频/WebView 占据整个可用屏幕，不显示在线人数、房间号、成员卡片或互动聊天。
- 点击视频区域才显示控制层；播放中无操作约 3 秒后隐藏。暂停、拖动进度、加载、缓冲和错误时控制保持可见。
- 横屏顶部只提供退出操作；底部覆盖层提供播放/暂停、时间、进度、弹幕、倍速和退出横屏/全屏，布局参考常见 B站全屏播放器。
- 断线、缓冲、硬同步和视频切换只使用短暂状态提示，不长期占据视频。


### 16.8 Alpha 9.2 真机验收与 GitHub 发布

2026-08-10，用户确认 Alpha 9.2 已完成真机测试并通过，可作为当前对外测试版本。

- 版本：`1.0.0-alpha.9.2`，`versionCode 11`。
- GitHub 标签：`v1.0.0-alpha.9.2`。
- APK 不提交进 Git 历史，作为 GitHub Release 附件保存。
- 当前版本继续使用 Debug 签名并标记为 prerelease，仅用于个人安装测试。
- 后续小修复提高 `versionCode` 并发布新标签，不覆盖或移动已经发布的标签。
- 互动表情和文字聊天继续作为后续迭代；旧 Alpha 10 本地片单已由账号与双人空间路线取代，不回填进 Alpha 9.2。

### 16.9 Alpha 9.3 观看控制释放

用户在 Alpha 9.2 日常使用中确认：观看页右上角“更多”菜单增加了记忆成本，且部分手机首屏无法立即看到全部播放控制。Alpha 9.3 采用方案 A：

- 顶部仅保留返回、房间状态和分享，移除“更多”。
- 第一排常驻播放/暂停、B站弹幕、倍速、横屏、全屏，所有按钮保持至少 48dp 触控高度和明确按下变色反馈。
- 第二排直接显示“换视频”和当前目标主题（浅色/深色），不再通过弹出菜单查找。
- 离开房间继续使用返回键加确认框，不在控制栏放置容易误触的危险操作。
- 互动表情仍按 D-034～D-038 继续规划，但在双方发送和显示链路完成前不展示无效按钮；未来接入第二排预留位置。
- 横屏与全屏仍为两个独立动作；横屏沉浸层继续遵循点击显示、播放中约 3 秒自动隐藏的规则。

设计真源：`design/alpha9-ui/watch-controls-options.html`。已验证截图：`watch-controls-options-comparison.png`、`watch-controls-option-a-light.png`、`watch-controls-option-a-dark.png`、`watch-controls-option-a-landscape.png`。
### 16.10 Alpha 9.3.3 播放器控制收敛与跳转保护

用户根据 Alpha 9.3.x 真机录屏确认：B站原生控制皮肤与 App 原生控制同时出现，横屏中央暂停按钮停留过久，并希望优先消除视频区域点击后离开 App 的风险。

- App 在竖屏和沉浸模式都使用透明原生交互层接管视频区域点击，点击不会直接传递给 B站页面元素。
- 竖屏点击视频区域通过播放器桥接切换播放/暂停；横屏/全屏点击仅切换 App 沉浸控制层显隐。
- B站原生进度条、中央播放/暂停按钮、结束推荐、标题作者、活动和推广覆盖层通过桥接样式隐藏，但视频与弹幕渲染层继续保留。
- App 沉浸控制层在播放中无操作约 2.8 秒后整体隐藏；播放时不显示中央暂停按钮，暂停或播放结束时才显示中央播放按钮。
- WebView 只允许无用户手势的可信播放器主文档导航；视频播放区域可见时，用户手势触发的所有顶层网页跳转均被阻止。
- 本轮不改变弹幕、倍速、同步协议、手动横屏和独立全屏产品规则。
### 16.11 Alpha 9.3.4 加载反馈与方向状态保护

基于测试录屏的补充审查，Alpha 9.3.4 在不改变观看页布局和协议的前提下补齐两个稳定性缺口：

- 房间视频加载也使用 8 秒慢加载提示和 20 秒可恢复提示；只有用户主动准备视频时才在 20 秒后进入准备失败流程，迟到加入或对方换视频不会被强制退回准备页。
- 横屏/竖屏切换前记录本机播放状态；播放中的方向切换会在播放器恢复后继续播放，暂停中的视频保持暂停。
- 方向切换产生的短暂本机 pause 事件在约 1.2 秒保护窗口内不广播，避免把 Android 旋转过程误判成房间暂停。
- 新增桥接 `__tongkanSetPlaying`，只用于方向切换后的确定性恢复，不改变普通播放/暂停同步协议。
### 16.12 Alpha 9.3.5 横屏控制层透明化

用户在 Alpha 9.3.x 横屏观看时反馈：底部整块黑色控制栏过于显眼，影响视频沉浸感。

- 横屏/全屏底部控制容器改为完全透明的覆盖层，不再使用整块半透明黑色背景。
- 进度条、时间和播放/弹幕/倍速/退出按钮仍保持可见；按钮自身继续保留独立的浅色/炭灰背景和按下态，保证对比度与触控反馈。
- 不改变控制层的点击显示、播放中约 2.8 秒自动隐藏、暂停/缓冲时保持显示规则。

### 16.13 Alpha 9.3.6 视频画面恢复

- Alpha 9.3.5 隐藏 B站原生控制层时误将 `.bpx-player-video-perch` 当作控制覆盖层；当前 B站 DOM 中该节点实际包裹 `<video>`，因此产生“有声音但无画面”。
- Alpha 9.3.6 只移除这一危险隐藏选择器，不恢复 B站原生进度条、中央暂停按钮、推荐覆盖层或网页跳转。
- 新增视频画面回归检查：竖屏、横屏、App 全屏和退出全屏后都必须同时有画面与声音。
- 用户已在真机确认 Alpha 9.3.6 视频画面恢复。
- 清晰度调查：未登录 B站的 Embed 当前只提供自动 360P；URL 强制 `quality/qn` 不生效，真实高清需要可选的官方 B站登录流程后选择账号可用的最高档位。

## 17. Alpha 10 路线调整：账号与双人空间优先

2026-08-11 用户确认先开发同看账号系统，并把唯一好友、共同片库、观看日历、历史和统计作为长期产品主线。旧“Alpha 10 先做纯 Android 本地片单”的顺序被 D-059 取代。

- Alpha 10.0：邮箱验证码账号和安全会话，首版通过 QQ 邮箱 SMTP 发信，并保留无需登录的匿名临时房间入口。
- Alpha 10.1：任意一方可单方面解绑；双方分别选择保留或删除自己的只读旧双人空间归档。
- Alpha 10.2：片库完全共享，双方权限一致，不增加个人私密片库。
- Alpha 10.3：日历确定为日期必填、开始时间可选、备注可选；未填时间显示“当天”，通知与重复计划后置。
- Alpha 10.4：共同观看时长按双方同时在线且实际播放的自然时间计算；暂停、缓冲、离线和匿名房间不计入。
- 账号头像第一版仅支持内置资源，等待用户提供候选图片后接入。
- Alpha 10.1：每人唯一好友绑定与“我们”空间。
- Alpha 10.2：双人共享片库和分类。
- Alpha 10.3：观看日历与当天列表。
- Alpha 10.4：共同观看历史和月度统计。
- B站登录与画质选择后置。
- 详细范围以 `ACCOUNT_PAIR_SPACE_PRD.md` 为准。
