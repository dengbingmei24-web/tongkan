# 同看（暂名）个人版 1.0 PRD

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 同看（暂名） |
| 版本 | 1.0 |
| 产品形态 | Web/PWA、Chrome/Edge 扩展、Android APK |
| 目标用户 | 产品所有者与一名固定朋友/伴侣 |
| 房间人数 | 固定 2 人 |
| 核心片源 | B站普通公开视频 |
| 辅助能力 | 合法视频直链同步、桌面与 Android 屏幕共享、语音、文字聊天 |
| 账号体系 | 无注册、无登录、临时昵称 |
| 部署原则 | 优先使用免费额度，接受少数网络无法建立 P2P 连接 |

## 2. 产品背景与目标

### 2.1 背景

现有同步观看产品通常存在以下问题：

- 平台自带的“一起看”要求注册账号，并受限于平台自己的内容。
- Teleparty、VideoTogether 等产品可以同步第三方播放器，但需要浏览器扩展。
- Watch2Gether、Kosmi 建房方便，但产品功能较重，且对国内内容生态适配有限。
- 普通屏幕共享覆盖范围广，但画质、延迟、带宽和 DRM 兼容性不如双方本地播放。

本产品是个人自用工具，不追求公开运营、商业化或大规模并发。产品以 B站双人同步观看为基础链路，以屏幕共享作为其他内容和异常场景的补充链路。

### 2.2 核心目标

1. 无需注册，生成邀请链接后即可让另一人加入。
2. 双方分别从 B站加载视频，服务器只同步播放状态，不转发视频文件。
3. 双方都可以播放、暂停、拖动、切换分P和切换视频。
4. 当内容无法通过播放器同步时，可切换至屏幕共享。
5. 桌面和 Android 均可发起屏幕共享。
6. 日常两人使用时不产生媒体服务器带宽费用。

### 2.3 产品口径

对外描述统一为：

> 无需账号。安装一次扩展或 Android App 后，点击邀请链接即可和对方同步观看 B站视频，或共享自己的屏幕。

不得使用“任何网络都能连接”“任何视频都能分享”“无需安装任何工具”等无法兑现的描述。

## 3. 调研结论与产品策略

| 产品/技术 | 可借鉴能力 | 本产品策略 |
| --- | --- | --- |
| Teleparty / VideoTogether | 扩展控制双方本地播放器 | B站同步采用同类扩展模式 |
| Watch2Gether | 无账号建房、链接邀请 | 保留极简房间流程 |
| Kosmi | 同步观看与屏幕共享并存 | 房间内提供两种观看模式 |
| Syncplay | 权威时钟、漂移修正 | 使用服务端播放锚点校准双方进度 |
| WebRTC | 点对点音视频传输 | 双人屏幕共享和语音默认 P2P |
| LiveKit / coturn | SFU 与 TURN 中继 | 1.0 不默认部署，仅保留配置接口 |

产品采用两条链路：

1. **B站同步链路**：双方各自加载同一个 B站视频，房间只同步控制状态。
2. **屏幕共享链路**：主持人捕获标签页、桌面或 Android 屏幕，通过 WebRTC 发送给对方。

## 4. 用户与核心场景

### 4.1 用户

- 用户 A：创建房间并发送邀请链接。
- 用户 B：通过邀请链接加入。
- 两人权限基本平等，均可控制播放和发起共享。
- 创建者保留结束房间、移除成员和刷新邀请链接的管理权限。

### 4.2 核心场景

#### 场景一：同步观看 B站视频

1. 用户 A 创建房间并粘贴 B站视频链接。
2. 系统解析视频标识并打开 B站播放页。
3. 浏览器扩展或 Android App 连接本地播放器与房间。
4. 用户 B 点击邀请链接并打开同一个视频。
5. 双方加载完成后进行三秒倒计时并开始播放。
6. 任意一方都可以暂停、播放、拖动或切换分P。

#### 场景二：共享桌面或标签页

1. 任意一方在房间中选择“共享屏幕”。
2. 对方确认切换模式。
3. 共享者选择标签页、窗口或整个桌面，并选择是否共享声音。
4. 对方观看实时画面，双方可继续语音和聊天。

#### 场景三：共享 Android 屏幕

1. Android 用户点击“共享手机屏幕”。
2. App 展示隐私说明并请求 MediaProjection 权限。
3. App 以前台服务运行并显示系统常驻通知。
4. 支持时，同时捕获系统声音；不支持时仅共享画面。

## 5. 产品范围

### 5.1 1.0 包含

- 双人私人房间。
- 随机邀请链接和临时昵称。
- B站普通公开视频同步。
- BV、AV 链接和多分P视频。
- 双方共同控制播放。
- MP4、WebM、HLS 等浏览器可直接访问的视频直链。
- 桌面 Chrome/Edge 标签页、窗口和屏幕共享。
- Android 整屏共享。
- 条件允许时捕获 Android 系统声音。
- 双向语音、文字聊天和表情反应。
- 网络状态、缓冲状态和连接诊断。

### 5.2 1.0 不包含

- iOS App。
- 三人及以上房间。
- 公开房间、好友系统或内容发现。
- 录制、回放或云端存储。
- 远程控制对方设备。
- B站直播、互动视频、付费内容、影视和 DRM 内容。
- 抓取、下载、代理或重新分发 B站视频文件。
- 默认 TURN、SFU 或媒体中继服务器。
- 应用商店和浏览器商店上架流程。

## 6. 功能需求

### 6.1 创建和加入房间

#### 创建房间

- 用户输入临时昵称后点击“创建私人房间”。
- 服务端生成：
  - 128 位随机房间 ID。
  - 128 位房主密钥。
  - 128 位邀请密钥。
- 创建成功后显示独立的“复制邀请链接”按钮。
- 房主密钥不得包含在邀请链接中。
- 房主身份保存在当前设备本地；清除本地数据后不提供找回。

#### 加入房间

- 用户 B 点击邀请链接后输入临时昵称。
- 房间已有两人时拒绝加入。
- 第二人加入后房间自动锁定。
- 房主刷新邀请链接后，旧邀请密钥立即失效。
- 被移除成员的连接和加入密钥同时失效。

#### 生命周期

- 双方在线时房间不设置主动时长上限。
- 双方均离开后保留 10 分钟，方便刷新和短暂重连。
- 空房超过 10 分钟后销毁全部状态。
- 不保存历史视频、聊天记录或屏幕内容。

### 6.2 B站视频识别

- 支持识别：
  - `BV` 视频链接。
  - `av` 视频链接。
  - 带 `p` 参数的多分P链接。
- 内部统一转换为 `BiliMediaIdentity`：
  - `bvid`。
  - `aid`（可获得时）。
  - `cid`。
  - `page`。
  - `title`。
- 双方必须处于相同的 `bvid + cid/page` 才能进入同步状态。
- 每名用户独立加载 B站内容、账号状态、清晰度和弹幕。
- 产品不得读取或存储用户 B站账号凭据。

### 6.3 桌面浏览器扩展

- 支持 Chrome 和 Edge Manifest V3。
- 权限仅覆盖：
  - B站播放页面。
  - 产品 Web 页面。
  - 扩展内部页面和本地存储。
- 产品 Web 页面通过 `externally_connectable` 将房间凭据交给扩展。
- 扩展在 B站页面中：
  - 定位当前可见、活动的 `HTMLVideoElement`。
  - 监听播放、暂停、拖动、播放速度和结束事件。
  - 执行远端播放命令。
  - 监听页面 SPA 路由和分P变化。
  - 注入轻量房间浮层。
- 如果页面中存在多个播放器，优先选择面积最大、可见且正在播放的元素。
- 无法确定唯一播放器时禁止同步，并提示用户手动选择或刷新页面。

### 6.4 Android B站播放器

- Android App 使用受控 WebView 打开 B站普通视频页面。
- 使用 JavaScript Bridge 获取和控制本地播放器状态。
- 页面能力检测失败时，不允许进入“已同步”状态。
- 登录、Cookie 和清晰度由 WebView 自身管理，产品业务服务不接触凭据。
- 不支持直接控制系统安装的官方 B站 App。

### 6.5 双方共同控制

#### 控制权限

- 双方默认都可以：
  - 播放。
  - 暂停。
  - 拖动进度。
  - 切换分P。
  - 切换至另一条 B站普通视频。
- 用户在 B站页面中打开新视频时，先询问“是否同步切换房间视频”。
- 只有确认后才更新房间片源。

#### 权威状态

服务端维护唯一的 `PlaybackAnchor`：

- 当前片源。
- 播放或暂停状态。
- 基准播放进度。
- 基准服务器时间。
- 播放速度。
- 最新操作序号。
- 最新操作者。

播放状态为播放中时：

```text
权威进度 = 基准进度 +（当前服务器时间 - 基准服务器时间）× 播放速度
```

#### 冲突规则

- 所有用户操作先发送至房间服务。
- 服务端为命令分配严格递增的房间序号。
- 双方同时操作时，序号较大的合法命令生效。
- UI 显示轻提示，例如“对方将进度调整到 12:36”。
- 播放和暂停立即提交。
- 拖动过程中只更新本地预览，松开进度条后提交最终位置。
- 每名用户每秒最多提交两次跳转命令。
- 远端命令执行期间设置 `applyingRemoteCommand` 标记，禁止把程序化事件重新上报。
- 每条远端命令记录序号，已经应用过的序号不重复执行。

### 6.6 同步与缓冲

- 客户端使用多次 Ping/Pong 计算与服务器的时钟偏差和往返延迟。
- 每 2 秒上报：
  - 本地进度。
  - 播放状态。
  - `readyState`。
  - 是否缓冲。
  - 当前片源标识。
- 进度修正规则：
  - 偏差小于 300ms：不处理。
  - 偏差 300ms—1.5s：短暂调整播放速度至 0.95—1.05。
  - 偏差超过 1.5s：直接跳转到权威进度。
- 任意一方连续缓冲超过 3 秒，房间自动暂停并显示“等待对方缓冲”。
- 双方恢复可播放后进行三秒倒计时并继续。
- 一方掉线时，另一方可以继续播放。
- 掉线者重连后直接同步到房间权威进度。

### 6.7 视频直链模式

- 支持浏览器可直接访问且允许跨域播放的 MP4、WebM 和 HLS 地址。
- HLS 使用 hls.js，在浏览器原生支持时优先使用原生能力。
- 播放前检测：
  - CORS。
  - 内容类型。
  - 编码兼容性。
  - 地址是否过期或需要认证。
- 无法播放时提示具体原因并建议使用屏幕共享。
- 不承诺支持带 Cookie、Referer、防盗链或临时签名的视频地址。

### 6.8 桌面屏幕共享

- 使用 `getDisplayMedia` 捕获标签页、窗口或整个屏幕。
- 正式支持桌面 Chrome 和 Edge。
- 优先引导用户共享浏览器标签页并勾选标签页音频。
- 同一时间仅允许一人共享。
- 切换共享者前必须停止当前共享流。
- 用户可以分别控制：
  - 屏幕画面。
  - 共享声音。
  - 麦克风。
- DRM 或系统保护导致黑屏时只显示解释，不尝试绕过。

### 6.9 Android 屏幕共享

- 使用 MediaProjection 捕获整个 Android 屏幕。
- 使用前台服务维持共享，并显示系统常驻通知。
- Android 10 及以上尝试通过 AudioPlaybackCapture 捕获系统声音。
- 系统声音属于条件能力：
  - 目标 App 可主动禁止捕获。
  - 部分国产系统可能限制后台录屏。
  - 锁屏、省电策略和切后台可能终止共享。
- 系统声音不可用时仍允许只共享画面。
- 麦克风和系统声音使用独立音轨。
- UI 推荐用户佩戴耳机，避免对方声音被麦克风重复采集。

### 6.10 语音、聊天与表情

- 支持双向语音。
- 支持麦克风静音和对方音量调节。
- 桌面扩展使用 Offscreen Document 维持语音 WebRTC 和房间连接。
- Android App 由原生客户端维护语音连接。
- 支持临时文字聊天和快捷表情反应。
- 聊天数据仅保存在当前房间内存中，房间销毁后删除。

### 6.11 模式切换

- 房间支持三种媒体模式：
  - B站同步。
  - 视频直链同步。
  - 屏幕共享。
- 任意一方申请切换模式时，对方收到确认提示。
- 对方确认后：
  - 停止当前媒体状态同步。
  - 停止现有屏幕共享流。
  - 更新房间模式。
  - 双方进入新模式准备状态。
- 语音和聊天在模式切换时保持连接。

### 6.12 网络诊断

- 显示：
  - 信令连接状态。
  - P2P ICE 状态。
  - 当前延迟。
  - 是否正在缓冲。
  - 屏幕共享分辨率和帧率。
- P2P 建连超过 15 秒后停止当前尝试并显示诊断。
- 默认提供的建议：
  - 双方切换到普通家庭 Wi-Fi。
  - 关闭 VPN 或代理。
  - 关闭后重新打开房间。
  - 更换由谁发起共享。
- 明确提示：“当前免费个人版没有 TURN 中继，部分移动网、校园网或公司网可能无法直连。”
- 配置中预留 TURN 地址、用户名和临时凭据接口。

## 7. 界面设计

### 7.1 视觉方向

- 深色影院风格。
- 黑灰背景、低饱和强调色。
- 主画面优先，减少会议软件式的复杂工具栏。
- 所有网络和权限提示使用人话，不暴露底层技术术语。

### 7.2 首页

- 主标题：“和重要的人，同步看同一个视频。”
- 主按钮：“创建私人房间”。
- 次入口：“加入房间”。
- 三项说明：“无需账号 · 固定两人 · 内容不会保存”。
- 首次使用时展示扩展和 Android App 安装入口。

### 7.3 房间准备页

- 显示双方昵称和在线状态。
- 提供 B站链接输入框。
- 提供“同步观看”和“共享屏幕”入口。
- 自动检测：
  - 桌面扩展是否安装。
  - Android App 能力。
  - 麦克风权限。
  - P2P 兼容性。
- 双方均准备完成后才能开始同步。

### 7.4 B站桌面浮层

- 扩展在 B站页面右下角注入可折叠浮层。
- 折叠状态显示：
  - 对方在线状态。
  - 同步状态。
  - 麦克风开关。
  - 展开按钮。
- 展开后显示：
  - 当前房间和视频。
  - 延迟和缓冲状态。
  - 文字聊天。
  - 表情反应。
  - 切换屏幕共享。
  - 退出房间。

### 7.5 Android 房间页

- 顶部显示对方状态和当前模式。
- 中部使用 WebView 或视频画面作为主舞台。
- 底部操作栏包含：
  - 播放/暂停。
  - 进度。
  - 麦克风。
  - 聊天。
  - 共享手机屏幕。
  - 模式切换。
- 聊天使用可上拉 Bottom Sheet。

### 7.6 屏幕共享页

- 16:9 主画面居中。
- 顶部显示共享者和网络质量。
- 底部显示停止共享、静音、全屏和切换模式。
- 系统声音不可用时显示非阻塞提示，不中断画面共享。

## 8. 技术架构

### 8.1 客户端

- Web/PWA：React、TypeScript、Vite、hls.js。
- 浏览器扩展：Chrome Manifest V3、TypeScript、Content Script、Service Worker、Offscreen Document。
- Android：React Native 裸工作流；Kotlin 模块实现 MediaProjection、前台服务和系统音频捕获。
- Web、扩展和 Android 共享房间协议、状态类型和校准算法。

### 8.2 服务端

- Web 静态资源默认部署至 Cloudflare Pages 免费额度。
- 房间信令默认使用 Cloudflare Workers + Durable Objects WebSocket。
- Durable Object 负责：
  - 房间成员。
  - 密钥认证。
  - 播放命令排序。
  - 权威播放锚点。
  - 临时聊天和缓冲状态。
  - 空房 TTL。
- 不使用持久数据库。
- Cloudflare 地址必须通过配置注入，业务协议不得依赖其专有接口，以便迁移至普通 Node.js WebSocket 服务。

### 8.3 音视频

- 使用 WebRTC `RTCPeerConnection`。
- 屏幕画面、系统声音和麦克风使用独立轨道。
- 双人媒体默认点对点传输。
- 服务端不接收、解码、录制或存储媒体内容。
- 默认仅配置部署时验证可达的公共 STUN。
- 预留自建 coturn 或第三方 TURN 配置。

### 8.4 安全与隐私

- WebRTC 媒体使用 DTLS-SRTP 加密。
- 房主密钥和邀请密钥通过 URL Fragment 进入 Web 页面，避免写入普通 HTTP 访问日志。
- 页面解析 Fragment 后通过 WebSocket 认证消息发送密钥。
- 房间 ID 与密钥均不可预测。
- 扩展只申请 B站和产品域名权限。
- 不读取浏览历史、其他网站内容、B站密码或 Cookie。
- 不提供录制功能。

## 9. 核心接口与数据结构

### 9.1 HTTP 与 WebSocket

- `POST /api/rooms`：创建房间。
- `WS /rooms/{roomId}`：建立房间实时连接。
- WebSocket 建立后第一条消息必须完成密钥认证。

### 9.2 核心类型

```ts
type RoomMode = "bilibili" | "direct-video" | "screen-share";

interface BiliMediaIdentity {
  bvid: string;
  aid?: number;
  cid?: number;
  page: number;
  title?: string;
}

interface PlaybackAnchor {
  media: BiliMediaIdentity | DirectMediaIdentity;
  paused: boolean;
  positionSeconds: number;
  playbackRate: number;
  anchoredAtServerMs: number;
  sequence: number;
  actorId: string;
}

interface ClientPlaybackReport {
  sequenceApplied: number;
  positionSeconds: number;
  paused: boolean;
  readyState: number;
  buffering: boolean;
  media: BiliMediaIdentity | DirectMediaIdentity;
  sentAtClientMs: number;
}

interface PeerCapabilities {
  platform: "web" | "extension" | "android";
  canControlBilibili: boolean;
  canShareScreen: boolean;
  canShareSystemAudio: boolean;
  canUseMicrophone: boolean;
}
```

### 9.3 主要事件

- `member.joined`
- `member.left`
- `member.removed`
- `media.change.requested`
- `media.changed`
- `playback.command`
- `playback.anchor.updated`
- `playback.report`
- `buffering.started`
- `buffering.ended`
- `mode.change.requested`
- `mode.changed`
- `screen.started`
- `screen.stopped`
- `chat.message`
- `room.ended`

## 10. 非功能需求

- 常见网络下信令连接应在 5 秒内完成。
- P2P 建连超过 15 秒必须给出失败结果，不无限等待。
- 视频同步偏差在稳定播放时保持在 500ms 内。
- 桌面屏幕共享目标端到端延迟不超过 1.5 秒。
- Android 连续共享两小时不得崩溃或出现持续累积的音画偏差。
- 服务端不得保存媒体内容。
- 任意客户端刷新或短暂掉线后应能恢复房间和权威进度。
- B站适配失败必须显式报错，不得显示虚假的“同步成功”。

## 11. 验收场景

### B站同步

- Chrome ↔ Chrome 普通 B站视频同步。
- Chrome ↔ Edge 普通 B站视频同步。
- 桌面扩展 ↔ Android App 同步。
- Android ↔ Android 同步。
- 多分P切换后双方进入同一分P。
- 双方同时播放/暂停，最终状态与服务端序号一致。
- 双方同时拖动，最终执行最后到达服务端的命令。
- 连续执行 20 次播放、暂停和拖动，不出现控制循环。
- 一方缓冲超过 3 秒，双方暂停；恢复后三秒倒计时继续。
- 一方刷新页面后恢复到当前权威进度。

### 屏幕共享

- 桌面标签页共享并包含标签页声音。
- 桌面整个屏幕共享。
- Android 整屏共享。
- Android 系统声音允许和禁止两种场景。
- 任意一方停止共享后，对方立即结束播放画面。
- Wi-Fi ↔ Wi-Fi、Wi-Fi ↔ 4G/5G 分别完成测试。
- 严格 NAT 导致失败时，15 秒内显示正确诊断。

### 房间与安全

- 第三人无法加入。
- 旧邀请链接在刷新密钥后失效。
- 被移除成员无法使用现有连接重新进入。
- 房主密钥不会出现在复制出的邀请链接中。
- 双方离开 10 分钟后房间状态被销毁。

## 12. 开发顺序

> 当前进度（2026-08-05）：阶段一桌面 B站基础链路和桌面屏幕共享基础链路已完成；Android 1.0 已进入可安装 Alpha 阶段（原生 WebView + 房间协议桥接，支持 B站播放器双向控制），待双真机验收后进入 Release。Android MediaProjection 和跨网络 TURN 仍保留在后续阶段。

### 阶段一：B站核心链路

- 房间服务和密钥体系。
- 权威播放状态和服务器命令序号。
- Chrome/Edge 扩展。
- B站普通视频、BV/AV 和多分P适配。
- 双方共同控制、漂移修正和缓冲等待。

### 阶段二：交流与 Android

- 语音、聊天和表情反应。
- Android App 和 B站 WebView 控制桥。
- 桌面与 Android 的跨端同步。

### 阶段三：屏幕共享

- 桌面 `getDisplayMedia`。
- Android MediaProjection。
- Android 系统声音条件支持。
- P2P 网络诊断和 TURN 配置接口。

### 阶段四：稳定性

- B站页面变化容错。
- 国产 Android 设备测试。
- 两小时连续运行测试。
- 安装包、扩展包和个人部署说明。

## 13. 已知限制与风险

1. B站页面结构变化可能使播放器适配暂时失效。
2. Android WebView 对部分 B站内容的行为可能与普通浏览器不同。
3. 公共 STUN 无法替代 TURN，部分网络环境不能建立 P2P。
4. Cloudflare 在中国大陆属于尽力而为，不保证稳定。
5. Android 系统音频能否共享由系统版本、厂商和目标 App 共同决定。
6. 手动安装扩展和 APK 会降低第一次使用的便利性。
7. 若未来要求稳定跨网络连接，需要增加付费服务器或 TURN 服务。

## 14. 1.0 完成标准

满足以下条件后视为个人版 1.0 完成：

- 两人在桌面或 Android 上可以无账号加入同一私人房间。
- 普通 B站视频可以稳定同步播放至少两小时。
- 双方均可控制播放，且同时操作不会产生无限同步循环。
- 桌面和 Android 均可以发起屏幕共享。
- 语音和聊天在观看模式切换时保持可用。
- 网络无法直连、系统声音不可捕获或 B站适配失效时，界面能给出准确解释和降级路径。
