# 同看浏览器兼容矩阵

- 基线提交：`6111f58`
- 状态含义：目标 = 应持续支持；条件支持 = 受浏览器或网络能力限制；未支持 = 当前版本不承诺
- 本文件定义测试边界，不代表所有组合已经完成真实验收

## 支持等级

| 平台 | 房间网页 | B站扩展同步 | 直链视频 | 屏幕共享 | 标签页音频 | 当前等级 |
| --- | --- | --- | --- | --- | --- | --- |
| Chrome 桌面版最近两个稳定版本 | 目标 | 目标 | 目标 | 目标 | 目标 | 一级 |
| Edge 桌面版最近两个稳定版本 | 目标 | 目标 | 目标 | 目标 | 目标 | 一级 |
| Chrome 隐身窗口 | 目标 | 条件支持，使用 split 扩展上下文 | 目标 | 目标 | 目标 | 二级 |
| Edge InPrivate | 目标 | 条件支持，需允许扩展进入 InPrivate | 目标 | 目标 | 目标 | 二级 |
| Firefox 桌面版 | 条件支持 | 未支持，当前为 Chrome MV3 扩展 | 条件支持 | 条件支持 | 浏览器差异较大 | 不承诺 |
| Safari 桌面版 | 条件支持 | 未支持 | 条件支持 | 条件支持 | 浏览器差异较大 | 不承诺 |
| Android / iOS 浏览器 | 条件支持 | 未支持 | 条件支持 | 未支持当前桌面链路 | 未支持 | 后续阶段 |

## 关键能力与降级

| 能力 | 依赖 | 不满足时的预期行为 |
| --- | --- | --- |
| 房间同步 | Fetch、WebSocket、Web Crypto | 显示连接错误，不进入假连接状态 |
| B站控制 | Chromium Manifest V3、内容脚本、B站 HTML5 播放器 | 提示扩展未连接或播放器适配失败 |
| 直链视频 | 浏览器原生媒体格式、CORS、无 DRM | 显示地址、编码或自动播放错误 |
| 屏幕共享 | `getDisplayMedia`、WebRTC | 禁用入口或在 15 秒内显示诊断 |
| 标签页音频 | 浏览器共享选择器和用户勾选 | 保留画面并明确提示无共享声音 |
| 严格 NAT | 可用 TURN | 无 TURN 时允许失败，但必须显示降级解释 |

## 发布前最小测试矩阵

| 编号 | 组合 | 网络 | 必测内容 | 结果 |
| --- | --- | --- | --- | --- |
| C1 | Chrome ↔ Chrome | 同一 Wi-Fi | 房间、B站双向控制、分 P、刷新恢复 | 未记录 |
| C2 | Chrome ↔ Edge | 同一 Wi-Fi | B站双向控制、扩展恢复、聊天 | 未记录 |
| C3 | Chrome 普通 ↔ Chrome 隐身 | 同机或同网 | split 上下文、席位隔离、扩展路由 | 未记录 |
| C4 | Edge 普通 ↔ Edge InPrivate | 同机或同网 | InPrivate 权限、席位隔离 | 未记录 |
| C5 | Chrome ↔ Edge | 不同网络 | 屏幕共享、连接诊断、停止共享 | 未记录 |
| C6 | Chrome ↔ Chrome | 严格 NAT + TURN | relay 建连和共享声音 | 未配置时跳过 |

每次真实执行请使用 `qa/TEST_RUN_TEMPLATE.md` 记录浏览器完整版本、操作系统、网络和结果；不要直接把“未记录”改成永久通过。
