# 同看可访问性只读审计

- 审计日期：2026-08-04
- 基线提交：`6111f58`
- 范围：Web 首页、加入页、房间页、自测页、扩展弹窗与 B站同步浮层
- 方法：静态语义、键盘路径、焦点样式、动态播报、减弱动画和颜色令牌检查
- 限制：未执行真实屏幕阅读器、浏览器缩放 400% 或自动化 axe 扫描；UI 变化后需要复测

## 结论

基础实现总体良好：页面使用原生按钮、输入框、标题和 label；主要输入错误使用 `role="alert"`；聊天和屏幕共享状态具备 `aria-live`；全局提供 `:focus-visible`；粗指针设备扩大了交互高度；`prefers-reduced-motion` 会缩短动画并移除进入位移。

按当前静态审查记录 2 项中风险和 3 项低风险。没有发现阻止鼠标用户完成核心流程的问题，但部分控件会误导键盘和辅助技术用户。

## 发现

### A11Y-01 中风险：存在可聚焦但没有行为的按钮

- 首页功能预览中的“播放预览”使用真实 `<button>`，但没有点击处理。
- 房间页“语音连接”显示“下一阶段”，仍是可点击且未禁用的按钮。

影响：键盘和屏幕阅读器用户会认为这些控件可以执行操作，激活后却没有反馈。

建议：纯展示元素改为非交互标记并从可访问树隐藏；未实现功能使用禁用按钮并提供说明，或改成普通状态行。

### A11Y-02 中风险：房间连接和播放通知没有统一动态播报

房间标题中的 `notice` 会显示连接、控制和错误状态，但容器没有 `role="status"` 或 `aria-live`。视觉用户能看到变化，屏幕阅读器用户不一定会及时获知断线、恢复或操作失败。

建议：为非紧急状态使用单一 `role="status" aria-live="polite"` 区域；错误继续使用 `role="alert"`，避免多个 live region 重复播报。

### A11Y-03 低风险：加入页的验证关联不完整

首页输入已使用 `aria-invalid` 和 `aria-describedby`，加入页昵称和无效邀请提示没有同等关联，无效邀请文本也没有 `role="alert"`。

建议：复用首页字段模式，为错误输入设置 `aria-invalid`、帮助文本 ID 和 `aria-describedby`。

### A11Y-04 低风险：画面双击手势只面向指针设备

直链视频支持在画面上连续点击切换播放状态，画面容器本身不可通过键盘触发。附近存在明确的播放/暂停按钮，因此核心操作仍可完成，但提示只描述“双击画面”。

建议：提示同时说明可使用播放按钮；不要把画面容器改成第二个重复按钮，除非能处理键盘语义和焦点顺序。

### A11Y-05 低风险：扩展弹窗动态状态没有 live region

扩展弹窗的 `#status` 会在脚本检查标签页后更新，但普通 `<p>` 没有 `role="status"` 或 `aria-live`。

建议：为状态元素添加 `role="status" aria-live="polite"`，并确保初始文本不会被重复播报。

## 已验证的正向控制

- 主要文字令牌对深色背景的计算对比度约为：正文 17.22:1、次级文字 13.03:1、中性文字 8.04:1、弱化文字最低约 4.58:1。
- 强调按钮文字与强调背景约为 8.30:1；错误、成功和焦点颜色对主背景均高于 6.6:1。
- `:focus-visible` 使用 2px 高对比焦点环，没有只依赖颜色区分连接和错误状态。
- 表单字段、聊天输入、进度滑块和多数图标按钮具备可访问名称。
- 屏幕共享状态和聊天记录使用 polite live region，表单和媒体错误使用 alert。

对比度为基于 `tokens.css` 中 OKLCH 值的静态计算；半透明叠层、视频画面上的控件和浏览器实际色彩管理仍需在真实页面复核。

## 建议顺序

1. 修正两个无行为按钮。
2. 为房间通知和扩展弹窗状态增加动态播报。
3. 统一加入页字段错误语义。
4. 使用 NVDA + Chrome、Narrator + Edge 完成一次真实键盘和播报验收。
