# 同看发布验收清单

每次准备交付测试版或正式版时复制一份本清单，填写版本信息并逐项验收。自动化测试通过只是前置条件，真实 B站播放器、浏览器权限、标签页声音和跨网络 WebRTC 必须人工验证。

每次实际执行的设备、网络、结果和失败证据请填写 [`qa/TEST_RUN_TEMPLATE.md`](../../qa/TEST_RUN_TEMPLATE.md)，本文件只维护通用检查项目。

## 版本信息

- [ ] Git 提交：`________________`
- [ ] 验收日期：`________________`
- [ ] 验收人：`________________`
- [ ] Web 地址：`________________`
- [ ] 信令地址：`________________`
- [ ] 设备 A：操作系统 / 浏览器 / 版本：`________________`
- [ ] 设备 B：操作系统 / 浏览器 / 版本：`________________`
- [ ] 网络组合：同一 Wi-Fi / 不同 Wi-Fi / Wi-Fi ↔ 移动网络：`________________`

## 1. 发布前自动化门禁

- [ ] Git 工作树干净，目标提交已经推送。
- [ ] GitHub Actions 的 `verify` 和 `integration` 作业均为绿色。
- [ ] `pnpm typecheck` 通过。
- [ ] `pnpm test` 通过。
- [ ] `pnpm build` 通过。
- [ ] `pnpm test:integration` 通过，包括 WebSocket smoke test 和七项双端验收。
- [ ] `apps/extension/dist.pem`、`.crx`、`.env` 与部署密钥未进入 Git。

### Alpha 10.2 生产发布证据（2026-08-18）

- [x] T041：全仓 typecheck、测试、集成测试、构建和依赖清单检查通过。
- [x] Account 48/48、Signaling 33/33、Web 16/16、Protocol 11/11、Extension 21/21 测试通过。
- [x] 共享片库 Preview 15/15、revision race 20/20、D1 级联清理通过。
- [x] 生产 D1 已应用 `0004_pair_archives.sql` 与 `0005_shared_library.sql`，迁移列表为空。
- [x] 生产 Account Worker 版本 `1dc75739-52bf-4db1-828e-6b11e70906c0` 已激活；健康检查 `testMode=false`，未登录片库请求返回 401。
- [x] Android 39/39、Lint 0 错误/19 警告、生产配置 Debug APK 构建通过；生产 API 与 FCM 已注入，Preview 测试令牌为空且已验证不在 APK 中。
- [ ] 双设备共享片库、FCM 通知、房间同步与长时间观看物理验收：用户当前没有第二位测试者，明确延期，不得标记通过。
- [ ] 长期 Release 签名：当前仍为 Debug 证书，仅供个人日常安装。

### Alpha 10.2.1 片库 UX 验收

- [x] Android JVM 测试覆盖直链、B23、无效输入、20 条上限、默认分类分组和 B23 失败文案。
- [x] Android 41/41 单元测试、Lint 0 错误/23 警告、生产配置 Debug APK 与 versionCode 37/versionName 1.0.0-alpha10.2.1 校验通过；未增加第三方图片或 UI 依赖。

### Alpha 10.2.2 B23 热修复验收

- [x] 单测验证默认 fetch 以 `globalThis` 为 receiver 调用，防止 Cloudflare `Illegal invocation` 回归。
- [x] 单测覆盖首个安全 302 直接收敛、200 HTML 中安全视频 URL 提取、原有不可信/过多跳转拒绝。
- [x] 本地 Workers Runtime 使用截图短链 `https://b23.tv/XM569Iw`，成功添加 `BV1SBbS6hEHa` page 1 且元数据 ready。
- [x] Android 合并分行分享标题和下一行链接，并提交规范化 URL；Android 42/42、Lint 0 错误/23 警告通过。
- [x] Account 51/51、全仓 132 项测试、typecheck、build、diff check 和 Preview Worker 上传通过。
- [x] versionCode 38 / versionName 1.0.0-alpha10.2.2 生产配置候选 APK 校验通过，Preview token 为空，SHA-256 已记录。
- [x] 经用户明确授权部署生产 Account Worker 版本 `1198e7ba-ff25-4aca-8369-105e2e1efa19`；100% 活动，健康检查 200/`testMode=false`，未登录片库 401 `AUTH_REQUIRED`。
- [ ] 安装 Alpha 10.2.2 后，用同一截图短链完成真机添加复验并确认标题/封面元数据。
- [ ] 添加面板贴底显示，输入每行实时出现“已识别 / 短链待解析 / 无法识别 / 超过上限”，存在无效行时不能提交。
- [ ] 服务端返回后逐条显示已添加、已存在或失败原因；失效/不可用 B23 不再只提示稍后重试。
- [ ] 默认片库按分类和未分类展示 16:9 封面；封面失败显示主题占位，不阻塞滚动和操作。
- [ ] 竖屏房间点击“换视频”后底部抽屉覆盖部分屏幕且视频仍可见；搜索、按分类浏览和手动粘贴入口可用。
- [ ] 横屏/全屏控制层提供“片库”，右侧抽屉打开时视频仍可见；选中条目后从 0 秒暂停准备，不自动播放。
- [ ] 匿名房间或未绑定用户仍可通过“换视频”进入手动链接流程，不因片库入口回归失效。
### Alpha 10.2.3 账号与片库修复验收

- [x] Account 53/53、Android 44/44、全仓 134 项测试、typecheck、build、真实信令集成与 Lint 0 错误/23 警告通过；片库 title PATCH、名称边界、自定义名称刷新保留和 JPEG 缩略图 URL 有回归覆盖。
- [x] Preview Worker `093b89a5-2bcd-42fe-b7a6-9e8c04c0bd8c` 与生产 Worker `62bc7034-2599-4519-bf3b-dd119e68dd67` 已部署；生产健康检查 200/`testMode=false`，未登录片库 401，远程 D1 无待应用 migration。
- [x] versionCode 39 / versionName 1.0.0-alpha10.2.3 生产配置 APK 已构建，生产 API 与 FCM 有效、Preview token 为空、源/交付 hash 一致，SHA-256 `C081BC427C8E1650374E48C711C937D2F620883EEA9F0E4E492A3A8BE89EE681`。
- [ ] 登录后点击“进入匿名模式”，首页显示匿名状态且账号会话仍保留；点击“返回账号”无需验证码即可恢复共同片库和好友页。
- [ ] 点击“切换登录账号”或“退出当前账号”后，邮箱和验证码表单恢复可用，可使用同一或其他邮箱重新登录。
- [ ] 解绑弹窗一次选择后服务端活动 pair 立即为空；keep 显示只读旧空间，delete 不显示本人归档访问。
- [ ] 从片库选择视频时创建房间、准备该视频并优先通知唯一好友；无设备 token 时显示复制链接/系统分享面板。
- [ ] 视频可重命名为 1–160 字符；刷新视频信息后自定义名称不被覆盖，封面显示 JPEG 缩略图或主题占位。

### Alpha 10.2.4 好友房间直达

- [ ] 解绑弹窗同时显示“解除并保留旧空间”“解除并删除旧空间”和“取消”，三项在当前真机主题下均可点击。
- [ ] 好友 A 登录并创建房间后，Account D1 只保存加密 invite URL，不出现 host key 或明文 invite key。
- [ ] 好友 B 停留首页时最多 10 秒看到“A 正在等你一起看”，点击后以 guest 身份进入同一房间，无需通知或粘贴链接。
- [ ] 创建者 A 的首页不显示加入自己的房间；第三账号、旧 pair 和已解绑双方均不可读取该记录。
- [ ] 新建房间覆盖同 pair 旧记录；房主离开后卡片消失；未显式离开时记录 10 分钟后过期并在读取时清理。
- [ ] 拒绝通知权限、无 FCM token 或推送失败时不弹出强制分享面板，好友 B 仍可从 App 首页进入。
- [ ] 活跃房间发布失败时房主收到复制链接/系统分享兜底，不影响已创建的临时房间。

### Alpha 10.3 日历集成、Preview、生产与视觉证据（2026-08-21，未发布）

- [x] W1 Account/D1、W2 Android 与 W3 QA 的批准提交已无冲突集成到 `codex/TK-004-calendar`；Calendar Preview QA 检查点 HEAD 为 `bf2318601401bff2007a28ea8307de2f4a788857`。
- [x] `pnpm typecheck`、`pnpm test`、`pnpm test:integration`、`pnpm build` 与 `pnpm deps:check` 通过；常规测试共 149 项，集成测试包含 localhost smoke 和七项真实双端场景。
- [x] Account 68/68 通过，包含真实本地 D1 migration、计划 CRUD/状态、月份/日期/今日排序、20 轮 revision race、keep 归档与级联清理。Windows 下测试文件改为 `vitest run --no-file-parallelism`，只避免 Wrangler registry 的 `EBUSY` 碰撞，不减少 D1 并发覆盖。
- [x] Calendar QA 16-case `-ValidateOnly` 离线通过，未读取 secret、创建 HTTP client 或发送网络请求。
- [x] 当前 Breath Tech UI 源码在 `C:\tmp\android-build\project-alpha10.3-breath-tech-production-20260821-142603` 通过 55/55 JVM、Lint 0 错误/26 警告与 `assembleDebug`；APK 为 versionCode 41/versionName 1.0.0-alpha10.3，v2 Debug 签名有效，生产 API 存在、Preview 主机不存在、测试令牌为空，SHA-256 为 `356301A7AD1A350F9E335DC64912F5835A2E72A59E6AF64FBA9EA73BC8704647`。
- [x] `git diff --check`、110 个 tracked Markdown 文件本地链接检查和 413 个 tracked 路径凭据扫描通过；另复核 3 个未跟踪 review 文件，固定测试值不属于生产凭据，生产凭据发现数为 0。
- [x] Preview D1 已应用 `0007_calendar_plans.sql`，Preview Account Worker 已部署为 `439139cb-ab48-4f72-a3f5-3012d6c37477`，健康检查为 HTTP 200/`testMode=true`。一次性 A/B/C Calendar Live 合同 16/16 通过，包含 20/20 revision race、归档权限和 D1 级联 `pair=0 state=0 plans=0`；夹具清理为 `users=0 pairs=0`，测试密钥已轮换。
- [x] 生产 D1 已在仓库外完整备份后应用 migration 0007，现有 pair/state 回填为 `1/1`；生产 Worker `17a59403-98bb-4652-9ad1-32a5ff5882db` 以 100% 生效并通过健康/鉴权冒烟。Alpha 10.3 生产配置 APK 已复制到 `release/`，SHA-256 为 `356301A7AD1A350F9E335DC64912F5835A2E72A59E6AF64FBA9EA73BC8704647`，尚未公开发布。
- [x] 用户确认 Breath Tech/nav-v3 真机视觉验收通过；当前视觉源包含首页状态卡、底部导航细线和可滚动贴底面板。
- [ ] 两台真实设备验证月历同步、计划新增/改期/完成/取消、首页“今天想看”、从计划进入房间和 keep 归档只读；这项与旧版 Alpha 10.2.4 日常使用矩阵仍是下一道门槛。

### Alpha 10.4 历史统计本地集成证据（2026-09-01，未部署）

- [x] Protocol 类型检查、13 项测试和构建通过；`history.bind`/`history.bound`、兼容错误码及播放报告新增字段已覆盖。
- [x] Account 类型检查、13 文件/93 项测试和 Wrangler dry-run 通过；真实本地 D1 覆盖 0001–0008 升级、幂等/乱序、UTC offset、解绑竞态、archive 和 pair 级联。
- [x] Signaling 类型检查、8 文件/67 项测试和 Wrangler dry-run 通过；fake-clock overlap、reload、stale、grant expiry、retry/alarm 与 fail-open 已覆盖。
- [x] Reviewer 为 `history.bind` 增加独立低频限流；该消息不消耗播放报告额度，也不延长房间活动时间。
- [x] Android 单元测试与 Lint 通过；已复验授权重试、旧 RoomClient 回调隔离、5 秒报告清理、`startedAt DESC, id DESC` 分页顺序和归档状态清理。
- [x] `qa/watch-history` PowerShell 解析、14 cases/13 tags ValidateOnly、JSON 解析与差异检查通过；runner 默认离线校验并拒绝生产地址，未包含凭据。
- [x] T406 非 APK 门禁通过：全仓 typecheck、210 项常规测试、信令 integration + Web 七场景自测、Extension bridge、Protocol/Web/Extension/Account/Signaling build、依赖清单、127 份 Markdown/46 个本地链接、451 个跟踪路径凭据扫描和 `git diff --check` 均通过。Account 测试与 Workers build 首次仅因沙箱 Wrangler registry/log 权限失败，沙箱外原样复跑通过。
- [ ] Android APK build 未获授权且未运行；不得复用旧 APK 作为 Alpha 10.4 证据。
- [ ] 配置 Preview Account Service Binding 与历史 Secrets 后，再单独申请 migration 0008、Preview Account/Signaling 部署和 History Live QA 授权。
- [ ] 生产 migration 0008、Account/Signaling 部署、APK build/publish、push 和公开发布必须分别获得授权。
- [ ] 两台真实设备验证可信计时、暂停/缓冲/Seek/倍速/断线边界、历史/月度、日历双标记和 keep archive；Alpha 10.3 延期矩阵仍不能视为通过。

## 2. 房间与邀请

- [ ] 房主创建房间后能复制访客邀请链接。
- [ ] 访客使用邀请链接加入后，双方昵称和在线状态正确。

### 好友推送邀请

- [ ] 两部支持 FCM 的 Android 设备均安装同一 FCM 构建并登录已绑定账号。
- [ ] Android 13 及以上首次启用时能显示通知权限请求，允许后邀请通知可到达。
- [ ] 好友 App 位于后台或锁屏时，点击“一起看”能收到包含邀请人和房间信息的通知。
- [ ] 点击通知能打开 `/room/{roomId}#join={inviteKey}` 并进入现有加入房间流程。
- [ ] 拒绝通知权限、设备无有效 token 或推送失败时，邀请方仍自动打开系统分享兜底。

### 账号与唯一好友

- [x] QQ 邮箱验证码登录成功后，主页显示账号状态且匿名创建/加入房间仍可使用。
- [ ] 关闭并重新打开 App 时立即显示缓存账号主页，后台校验期间不阻塞首屏；明确鉴权失败后才返回登录页。
- [ ] 创建好友邀请码、复制、另一账号接受后，双方“我们”页都显示同一活动绑定。
- [ ] App 重启后唯一好友绑定能够从服务端恢复，未绑定账号不能调用好友邀请接口。
- [ ] 点击“邀请一起看”能创建房间并触发通知优先、系统分享兜底；访客深链只能使用访客密钥。
- [ ] 退出登录后本地 Session、缓存账号和已注册设备 token 均被撤销；重新打开 App 不恢复已退出账号。
- [ ] 非空 `tongkanAccountTestAccessToken` 未同时启用显式允许开关时，Android 构建必须失败。
- [ ] 访客链接不包含房主密钥。
- [ ] 第三个浏览器无法占用第三个席位。
- [ ] 页面刷新后能恢复身份、房间和最新权威播放状态。
- [ ] 错误或过期密钥会显示明确提示，不会进入假连接状态。

## 3. 真实 B站双端同步

- [ ] Chrome ↔ Chrome 完成一次完整观看流程。
- [ ] Chrome ↔ Edge 完成一次完整观看流程。
- [ ] 房主播放、暂停、拖动和改倍速时，访客正确跟随。
- [ ] 访客播放、暂停、拖动和改倍速时，房主正确跟随。
- [ ] 双方近似同时操作后，最终状态与服务端最新序号一致。
- [ ] 切换 BV 后复用或打开正确视频页，双方进入同一视频。
- [ ] 切换分 P 后双方进入同一分 P。
- [ ] 一方缓冲超过阈值时双方暂停，恢复后能够继续。
- [ ] 连续执行 20 次播放、暂停和拖动，不出现控制回声或无限循环。
- [ ] 关闭并重新打开扩展后台后，房间页与 B站标签页路由能够恢复。

## 4. 直链视频

- [ ] 双方加载同一个公开 MP4 或 WebM 地址。
- [ ] 播放、暂停、拖动和倍速同步正常。
- [ ] 无效、跨域受限或浏览器不支持的地址会显示可理解的错误。
- [ ] 从直链模式切回 B站模式后，权威状态正确。

## 5. 桌面屏幕共享

- [ ] 共享浏览器标签页时，对方能看到画面。
- [ ] 勾选“共享标签页音频”后，对方能听到声音。
- [ ] 共享窗口和整个屏幕均能建立连接。
- [ ] 共享者主动停止后，对方画面立即结束并回到观看模式。
- [ ] 浏览器原生“停止共享”按钮与产品按钮行为一致。
- [ ] 双方可以轮流发起共享，同时只能存在一个共享者。
- [ ] 同一 Wi-Fi 下完成一次共享。
- [ ] 不同网络下完成一次共享；如果直连失败，15 秒内显示明确诊断。
- [ ] 配置 TURN 时，严格 NAT 网络能够通过 relay 建连。

## 6. 聊天、错误与恢复

- [ ] 双方发送聊天消息时昵称、顺序和内容正确。
- [ ] 登录用户的“我们”页显示昵称、脱敏邮箱和同看 ID；修改昵称后重启 App 仍保持，并在新进入的房间消息中生效。
- [x] 竖屏打开输入法后，顶层输入栏始终紧贴键盘上方；正在输入的文字、光标、连续删除反馈和 App 内“发送”按钮全部可见。
- [x] 点击系统键盘“发送”、物理回车或 App“发送”按钮均走同一发送路径：只生成一条本地气泡，成功后清空输入，失败时保留输入内容并提示。
- [x] 键盘关闭后输入栏回到底部聊天卡片位置；进入横屏、网页全屏、入口页或离开房间时不会残留顶层输入栏。
- [ ] 横屏聊天关闭时收到对方消息，消息按钮显示未读红点；打开聊天面板后红点消失。
- [ ] 信令服务未启动或连接中断时显示明确错误。
- [ ] 短暂断网后重新连接，播放状态不会倒退。
- [ ] B站页面结构不兼容时显示失败提示，不显示虚假的“同步成功”。
- [ ] 屏幕共享失败、无音频或权限被拒绝时给出对应降级建议。

## 7. Android Alpha 9.3 观看页

- [ ] 启动后先进入房间入口页，顶部安全区不遮挡标题和按钮。
- [ ] 进入观看页后，顶部只显示返回、房间状态和分享，不再出现三点“更多”按钮。
- [ ] 播放、弹幕、倍速、横屏、全屏五个按钮在首屏完整可见，文字和图标没有挤压或截断。
- [ ] “换视频”和浅色/深色主题按钮在第二排直接可见，点击后有明确按下变色反馈。
- [ ] 切换主题后第二排按钮文案和图标正确更新，视频和房间连接不中断。
- [ ] 点击横屏进入铺满视频的沉浸观看；点击全屏不强制改变方向。
- [ ] 横屏控制层点击显示，播放中约 3 秒自动隐藏；弹幕、倍速、竖屏和退出全屏均可用。
- [ ] 返回键在全屏时优先退出全屏；普通观看页返回时显示离开房间确认。
- [ ] 在至少一台 390×844 左右设备和一台带刘海/灵动岛设备上完成浅色、深色、竖屏和横屏检查。

## 7.1 Android Alpha 9.3.2 P0 缓冲与同步稳定性

- [ ] 短暂缓冲少于 2 秒时，房间不会联动暂停，也不会持续显示明显缓冲提示。
- [ ] 同一方持续缓冲超过 2 秒时，只触发一次房间暂停，另一方不会收到重复暂停锚点。
- [ ] 缓冲恢复后只上报一次恢复状态，房间保持暂停，必须由任意一方手动播放后继续。
- [ ] 连续重复 5 次“短暂缓冲 / 长缓冲 / 恢复”，没有重复上报、状态卡死或播放暂停循环。
- [ ] 缓冲防抖等待期间切换视频或离开房间，不会在旧视频或旧房间补发延迟缓冲报告。
- [ ] 本机与房间进度偏差超过 1.5 秒并被硬同步时，竖屏状态区短暂显示“已重新同步 · 偏差 X.X 秒”。
- [ ] 横屏沉浸模式在缓冲期间保持控制层可见，缓冲恢复且手动播放后继续执行约 3 秒自动隐藏。
## 7.2 Android Alpha 9.3.3 播放器控制与跳转保护

- [ ] 竖屏点击视频区域只通过 App 桥接切换播放/暂停，不显示 B站原生进度条、中央暂停按钮或推荐入口。
- [ ] 竖屏连续点击视频区域 20 次，WebView 顶层 URL 不变化，App 不跳转到 B站视频页、活动页或推荐页。
- [ ] 横屏/全屏播放时，App 控制层显示后约 2.8 秒整体隐藏，播放状态下中央暂停按钮不常驻。
- [ ] 暂停、播放结束、缓冲、拖动进度或加载错误时控制层保持可见，中央仅显示“播放”按钮。
- [ ] 点击横屏视频区域只切换 App 控制层显隐，不把点击传递给 B站原生播放器皮肤。
- [ ] 通过 B站推荐、标题、作者、活动覆盖层和 `window.open` 触发的顶层导航均被阻止；视频、弹幕和同步桥接继续正常工作。
- [ ] 进入横屏、退出横屏、进入全屏和退出全屏后，原播放/暂停状态与进度保持不变。
## 7.3 Android Alpha 9.3.4 加载与方向状态

- [ ] 房间视频加载超过 8 秒时显示明确的“B站播放器加载较慢”状态，不再只显示黑屏和“已暂停”。
- [ ] 房间视频加载超过 20 秒时显示“可点击换视频重试”，不会把已加入房间的用户强制带回错误准备页。
- [ ] 视频正在播放时点击横屏或退出横屏，播放器最终仍保持播放；视频原本暂停时不会被自动播放。
- [ ] 横屏切换产生的临时 pause 事件不会广播成房间暂停，也不会让另一台设备停下。
## 7.4 Android Alpha 9.3.5 透明横屏控制层

- [ ] 横屏/全屏显示控制时，底部没有整块黑色背景，视频画面仍可连续看到。
- [ ] 时间、进度条、播放、弹幕、倍速和退出按钮保持清晰可见且可点击。
- [ ] 浅色和深色主题下，单个按钮仍有足够对比度和明确按下态。
- [ ] 控制层隐藏后只保留视频画面和弹幕，不留下黑色底栏残影。
## 7.5 Android Alpha 9.3.6 视频画面回归

- [ ] 进入房间并开始播放后，视频画面和声音同时正常，不能出现只有声音、黑屏或只显示弹幕。
- [ ] 竖屏、手动横屏、App 全屏和退出全屏后，真实视频画面均持续可见。
- [ ] B站原生控制栏、中央暂停按钮和推荐覆盖层仍保持隐藏，不因恢复画面重新出现。
- [ ] 点击视频不会跳转 B站网页，App 播放、弹幕、倍速和进度操作仍正常。

## 8. 稳定性与发布结论

- [ ] 双端持续观看至少 30 分钟，无明显漂移、内存持续增长或频繁重连。
- [ ] 正式版候选持续观看至少 2 小时。
- [ ] 浏览器控制台没有持续刷新的异常或未处理 Promise rejection。
- [ ] 已记录所有未通过项的复现步骤、截图、浏览器版本和网络环境。
- [ ] 阻断发布的问题数量：`____`
- [ ] 已接受的已知限制：`________________________________`
- [ ] 结论：允许发布 / 修复后复测 / 不允许发布
